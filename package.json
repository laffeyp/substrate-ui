{
  "name": "substrate-ui-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Live end-to-end test harness for substrate-ui — drives the real reveal shell in real Chrome (Playwright channel:'chrome') against the real server. The observation contract's tooling, scoped to this repo and pinned by package-lock.json so it is reproducible, not an ad-hoc /tmp install. Phase 6 (2026-09-22) retired the classic tree to _deprecated/classic-tree/; the reveal shell is the only entry.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && npm run lint && vite build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint web/",
    "smoke:vm": "npx tsx harness/vm_smoke.ts",
    "shakeout": "npx tsx harness/shakeout/run.ts",
    "pixel:baseline": "npx tsx harness/pixel_baseline.ts",
    "pixel:diff": "npx tsx harness/pixel_diff.ts",
    "test:unit": "npx tsx --test web/reveal/__tests__/*.spec.ts"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/pngjs": "^6.0.5",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@typescript-eslint/eslint-plugin": "^8.70.1",
    "@typescript-eslint/parser": "^8.70.1",
    "eslint": "^10.11.0",
    "pixelmatch": "^7.2.0",
    "playwright": "^1.49.0",
    "pngjs": "^7.0.0",
    "tsx": "^4.23.12",
    "typescript": "^5",
    "vite": "^5"
  }
}
