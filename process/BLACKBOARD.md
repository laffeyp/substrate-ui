# BLACKBOARD.md — substrate-ui

*Single writer per section. Discipline, not code-enforced. See `../sdd-kit-2/AGENTS.md` § The BLACKBOARD protocol. The Architect reads `## Surfaced for review`; the Agent surfaces what matters there plus plain-English summaries in chat.*

*Instantiated 2026-06-17 as the artifact-discipline retrofit ruled by review #39. Rounds #30–#38 predate this board; their audit trail is the review envelopes (`../.review-pipe/resp-03*.txt`) + the 5 "UI BUILD" entries currently in `../substrate/BLACKBOARD.md ## Built` (pointed-to below, to be physically relocated at the next substrate housekeeping pass).*

---

## Surfaced for review

*Agent + Architect. Halts, partials, comprehension affirmations, Rubber Duck observations marked `surfaced`.*

- **[RESOLVED 2026-08-16 via v0.2 lock; see ## Built same date] 2026-08-15 Claude — awaiting_architect_decision: v0.1 lock has no `## Decisions` entry.** `signals/versions/0.1.json` reads `"locked": true, locked_at: "2026-08-14", locked_by: "Architect (Peter) ratified 2026-08-14; agent-drafted by Claude Opus 4.7 same day."` The `locked_by` string is agent-written prose; no matching `## Decisions` entry from the Architect records the ratification. Sprints 020 through 029 dispatched against the lock in that state. Under AGENTS.md hard rule 12 the ratification is the sign-off; the missing `## Decisions` line is the rule-12 gap the 2026-08-15 review's F5 named. Architect ruling: post the ratification to `## Decisions` (naming v0.1 as the locked contract), or reset `signals/versions/0.1.json` to `locked: false` and pass Sprint-0 formally before any Wave-2 dispatch.

- **[RESOLVED 2026-08-16 via v0.2 lock; see ## Built same date] 2026-08-15 Claude — vocabulary_change_required: eight pending proposals for v0.2.** Four from `signals/versions/0.1-rationale.md § Open proposals for v0.2`, four from `REVIEW-2026-08-15-vocab-mapping-to-substrate.md § F1` (the four Layer-0 entity merges the current v0.1 silently collapses with substrate's v0.2). No `## Decisions` entry rules on any of them.
  1. `NEW_TAG_PROPOSED: INSPECTOR_RENDERED` — inspector paint tag. Evidence in Wave-1: the inspector renders as a single innerHTML block fed by EVENT_INSPECTED (line 725, `web/app.ts`) and PRODUCER_INSPECTED (line 745); no sub-pane structure surfaced across 12 sprints.
  2. `NEW_TAG_PROPOSED: FRAME_RENDERED` — sampled ambient-stratum paint-cadence tag. Evidence in Wave-1: `_paintFrame` counter graded monotonic across 220–261 paints per fixture (Sprints 022–028) with no cadence-related invariant violation observed.
  3. `PAYLOAD_FIELD_PROPOSED: TURN_SUBMITTED.turn_index required`. Evidence in Wave-1: the single call site at `sendChatMessage` (line 316, `web/app.ts`) always sets `turn_index = Math.floor(convo.length/2)`. No mid-conversation-reset call site exists today.
  4. `TAG_SPLIT_PROPOSED: TOPOLOGY_LAUNCHED vs BUNDLED_LAUNCHED vs AUTHORED_LAUNCHED`. Evidence in Wave-1: `/api/launch` (bundled) emits `TOPOLOGY_LAUNCHED`; `/api/build` (authored, Studio path) does not emit any launch tag; the two paths are structurally distinct and currently share one tag by omission.
  5. `ENTITY_MERGE_PROPOSED: Record ↔ Run`. Evidence: `REVIEW-2026-08-15-vocab-mapping-to-substrate.md § F1` names the merge and cites `grammar/PRINCIPLES.md § supervised-grammar-evolution taxonomy` requiring explicit filing. The current vocab uses `Record` where substrate uses `Run`; they refer to the same persisted object exposed via `/api/records/<name>`.
  6. `ENTITY_MERGE_PROPOSED: Topology ↔ Topology`. Evidence: same review, same table. Same primitive, same namespace, same `substrate.topologies.bundled` registry.
  7. `ENTITY_MERGE_PROPOSED: AgentRun ↔ Run` (the substrate Run whose topology is a tool-loop). Evidence: same review; the rationale doc's `AgentRun` entity IS a substrate Run with a specific topology shape.
  8. `ENTITY_MERGE_PROPOSED: Event ↔ Envelope`. Evidence: same review; the `event` the user clicks in `EVENT_INSPECTED{seq, kind, ...}` IS the substrate Envelope with fields `seq`, `kind`, `payload`.
  All eight are surfaced. Under the standing feedback rule (`feedback-report-do-not-prescribe`), the Agent files no recommendation; the Architect resolves via `## Decisions`.

- **2026-06-23 Claude Code — SESSION CLOSE: the real tool suite (no-permissions-by-default) + the terminal/content/legibility console surfaces + sprint 012 drift polish; forward work specced in `process/ROADMAP.md`.** A long build session across both repos. **What shipped:** **(1) substrate — the `tool_loop` real tool suite** (`topologies/tool_loop/tools.py`), designed by READING THE SOURCE of opencode/Cline/aider (via gh, not blog summaries): typed tools, errors-as-observations, capped output, surgical `edit_file`; suite = PURE calculator (deterministic CI) + READ-ONLY (read_file/list_dir/grep/web_fetch) + WRITE/EXEC (edit_file/write_file/bash). **Architect philosophy baked in: NO PERMISSIONS BY DEFAULT** — full autonomy (the references gate mutation; this inverts that); approval is opt-in via `pause_await_input`, reflecting the end-state where the LLM runs these topologies itself. **The duplex reviewer earned its keep (#51/#52):** caught that the committed tool_loop record was STALE — it replayed but no longer REGENERATED byte-identically after an `int->Any` widening moved the embedded schema — folded with a CURRENCY GATE (fresh-vs-committed `first_divergence` per bundled record) + made errors-as-observations airtight (pre-validate output encodability inside the try) + confirmed the no-permissions design coherent; review #52 confirmed the folds + caught a latent gate edge (guarded against non-deterministic bundled topologies). **(2) substrate-ui surfaces:** the integrated **terminal** (read commands `cat`/`narrate`/`tail`/`inspect`/`ls` + seq/time pairing); **content views** (the inspector renders code legibly + clickable output artifacts); **run-as-graph legibility** (split each lane into queued|ran, spawn dot at the run-START — diagnosed by correlating `run_graph` with the screenshot). **(3) sprint 012 drift polish** (all four items + 2 real bugs found): rAF replay (decouple advance/render, honest 4/30/120/480 speeds), rail grouping (demos vs your-runs, newest-first) + a prune (`clear` + `/api/runs/clear`, cleaned the 231-run detritus, fixed a source mis-tag), the `game_of_life_glider` fixture closing the pixel-decode mirror-blind spot (+ a harness race fixed at root). **STATUS — both repos green + pushed:** substrate ≈327 passed/1 skip + currency gate; substrate-ui 25 server tests + e2e + pixel-decode. **FORWARD WORK is fully specced in `process/ROADMAP.md`.** **OPEN ARCHITECT RULINGS (need a decision):** (a) the **interactive open-source-model agent** — the north star — is GATED on a usage/theory pass (interaction model, topology shape, natural-language tool-calling) BEFORE any code; (b) the static **topology-structure view** (`/topology_graph` built but unrendered) — wire or defer?; (c) substrate **repo-root signposting** (canonical vs history); (d) the **CliResponder** seam (which model backs the interactive agent). **DISCIPLINE NOTE:** drift items B/C were built code-first before a sprint card — the Architect flagged it ("follow SDD style"); corrected with `sprint-012-drift-polish.md` declaring the contract for C2/A. Nothing `halted`.

- **2026-06-17 Claude Code (Opus 4.8, 1M)** — COMPREHENSION_AFFIRMATION (recorded as substrate-ui comes under formal kit discipline). This project is **substrate-ui**, the read + thin-control console over the **Substrate** runtime. It is a SEPARATE CONSUMER of substrate, reaching it ONLY through the public `substrate.api` read seam (F-API-6) — a small stdlib-`http.server` + msgspec backend (`server.py`) serving a vanilla-JS console (`web/`), plus the Studio build seam (`builder.py`: an authored JSON Topology spec → a real `TopologyBuilder` topology → a genuine recorded run). **What SDD is at root, for this project:** the lossy human-description step is replaced by reading the runtime's own typed record directly — the UI does not narrate what a run "did", it projects the actual sequenced events (`read_record`, `run_graph`, `narrate`, `explain_producer`, `first_divergence`, `view_at`) the runtime already emitted. The UI emits NO signals of its own, so it locks no vocabulary; the contract that binds it is the eight-word tone canon (Producer, Bus, View, Predicate, Trigger, Route, TerminationPolicy, Topology — no anthropomorphism, no marketing reframes), verified clean by a standing grep. **The kit loop here:** from the Studio increment forward, each increment gets a sprint card with a declared dual + observation contract BEFORE code; build → `test_server.py` (real server/real api over HTTP) → live `e2e_console.js` (real Chrome) → independent review via the duplex-pipe reviewer → fold. **The hard rule binding me:** halt-and-articulate, and never present looks-done-but-inert as done — review #39 caught exactly that (a live Studio seam with zero tests because no card forced its artifact contract); the retrofit closes it.

- **2026-06-19 Claude Code — CHAPTER: review-application + ui-backend-6 + reorg + readability + CI (reviews #44–#48; supersedes the post-#43 status).** A long pass on the console, all on the real runtime. **What shipped:** **(1) REVIEW.md (the UI half)** — server SECURITY hardening (a CSRF Origin-vs-Host check on launch/build/resume; a path-traversal guard on record names; a concurrency cap on spawned runs; 500 bodies no longer leak tracebacks; `_static` containment via `relative_to`, not `startswith`) + the stored-XSS fix (`escapeHtml` now escapes quotes; every record-derived sink wrapped); the Studio authoring traps (PerKey removed — it crashed the build with a raw TypeError; a PerKindLatest predicate now raises SpecError instead of silently comparing field-count; spurious `unfired_triggers` guarded to terminal runs; a staleness guard on `renderDiff`/`inspectProducer` so a slow fetch can't overwrite a switched-to record); the builder offload through `call_responder` (a real model call no longer blocks the loop) + a malformed spec now raising the promised SpecError. **(2) ui-backend-6 — authored Routes are now CONSUMED.** A Trigger can declare `reads: <slot>`; its input_builder feeds the Route's staged data into the Producer it starts, and authored Producers are input-aware (a model Producer folds its input into the prompt; a stub reflects it) — before, every authored Route was a visible-but-inert edge. The Studio trigger form gains a `reads` field; a test proves the routed Critique reaches the Fixer's input. **(3) REORG** — the loose JS/docs grouped off the root into `web/ harness/ process/ tests/`. **(4) LICENSE (Apache-2.0)** added (was promised in prose, absent on disk). **(5) CI** — a NEW workflow: lint + server-tests + real-Chrome Playwright e2e, with a cross-repo PRIVATE checkout of `laffeyp/Substrate` via a fine-grained PAT; green. **(6) pipe review #48 (readability vs White/Orwell, for OSS)** — applied: the README lede rewritten to one plain what-is-this sentence, the SDD disclosure demoted out of the lede into "How it was built", the run-block deduped, the one sales-voice line ("the product is complete") swapped for the factual form. **Tested-live throughout:** 24 server tests + both e2e (console + Studio) PASS; pushed to `laffeyp/Substrate-Ui`, CI green. Nothing `halted`. (The substrate-side of the same review is in `../substrate/process/BLACKBOARD.md`.)

- **2026-06-17 Claude Code — WIDER PERCEPTUAL RE-VERIFICATION (Architect-directed) + a missing-surface finding.** Per the Architect's "re-verify more of the existing UI — make sure nothing else hid behind green DOM," captured + VIEWED 6 more dynamic/edge states (`capture_states.js`, `npm run capture:states`): torn→amber INCOMPLETE, event-click inspector (distinct from lane provenance), seq-0 edge, live-launch ● LIVE pulse, live→FINALISED transition, resume→FINALISED with stage1 interrupted (amber dashed) vs stage2 completed (green). ALL render correctly; the inspector-clear fix holds across every frame; NO new stale-class bugs. The pass DID surface one real gap for an Architect ruling (below) + two LOW minors (drift watchlist). The headline: **`/topology_graph` is built + tested but the console never renders it (`grep topology_graph web/app.js` = 0).** The header reads "TOPOLOGY · RUN-AS-GRAPH" but only the dynamic run-as-graph ships; there is NO static topology-structure view, though the design handoff §6 promised BOTH graph surfaces and `topology_graph()` was built as the de-risk projection FOR this. A working, tested endpoint with no UI consumer. **Architect ruling needed:** wire the static topology-structure view into the console as the next increment, or defer it? (Not a bug in what's shown; a promised surface missing from the UI.)

- **2026-06-17 Claude Code — REVIEW #43 (understandability / approachability / organization / kernel-vs-app separation; non-leading) + FOLD + 1 surfaced for the Architect.** Verdict: well-organized, unusually well-written, graspable in one sitting; the kernel/app separation INSIDE substrate is REAL + mechanically enforced (the reviewer verified: no kernel→reference imports; api.py a genuine thin facade; F-API-6 a CI import-linter gate + AST test). Findings: **(2, accuracy — FOLDED) substrate-ui/README overstated "substrate.api ONLY"** — the import graph shows it also uses the public `substrate.reference` Responders + `substrate.topologies.bundled` ("no kernel internals" true; "api only" false). Fixed the README's three "only" claims to name the real public surfaces. The exact claims-must-match-code principle, biting my own doc. **(3, boundary — FOLDED) the UI→substrate boundary was convention + a grep** (substrate's own boundary is a CI gate) → added `test_ui_imports_only_sanctioned_substrate_surfaces` (AST over substrate-ui/*.py — only substrate.api/reference/topologies, never a kernel internal); passes; the boundary is now mechanically enforced. 23 server tests. **(1, SURFACED for the Architect — touches the PUBLISHED substrate runtime repo, not mine to restructure):** the substrate REPO ROOT buries load-bearing material under the SDD/spec audit trail (multiple superseded spec drafts across 4 spec dirs + signals/ sprints/ proof/ archive/), with nothing at first contact stating what's canonical (product draft7+A1/A2, kernel v15) vs history — the worst understandability/approachability obstacle. Reviewer's fix: a README "canonical specs / the rest is history" block (cheap, safe) and/or relocate superseded drafts under `specs/history/` (a restructure of the published repo — honors no-deletion via relocation). **Architect ruling needed:** do the README signpost block on substrate/, the relocation, both, or neither? (Also the reviewer's cleaner option for #2: re-export DeterministicResponder/OllamaResponder onto substrate.api so the UI's model seam is literally api-only — a small addition to substrate's public surface; the Architect's call on the runtime repo.)

- **2026-06-17 Claude Code — REVIEW #42 (canvas + model Producers, sprints 005–007) + FOLD. Verdict: REAL and HONEST.** The independent reviewer verified by running: the model seam genuinely calls the runtime's OWN DeterministicResponder (byte-for-byte `stub[0]:dbcdd5257900`), Ollama FAILS LOUD as ProducerFailed (no silent stub), `deterministic=` is replay-honest, the canvas is a faithful `buildSpec()` projection, and the two-track perceptual discipline is REAL (the reviewer ran the capture harness AND looked). No regression (22 server tests; observe+control E2E green). The reviewer caught + corrected a near-false-alarm on themselves (producer-level vs spec-level responder). Two findings, BOTH FOLDED: **(2, LOW honesty) canvas Route edges over-stated connectivity** (drawn to every triggered Producer; the stub consumes no slot) → fixed: Routes now draw to a SLOT NODE (`slot · <name>`), of-emitter → slot, with an honest caption (the consuming Producer is the author's intent, not drawn); structural assertion + viewed (frame 34: dashed `stage-crit` edges reviewers→slot, solid `adjudicate` edges reviewers→judge — distinct + honest). **(3, MINOR UX) responder form couldn't set model_name/seed** → added seed + model inputs (`spec.seed`/`spec.model_name`); verified seed=7 builds. Disposition: both `resolved-here`. The dangerous class (model-seam-ships-a-fake) was the top concern and it's CLEAN.

- **2026-06-17 Claude Code — PERCEPTUAL-TRACK DISCIPLINE FAILURE (Architect-caught) + FIX + a real bug it found.** After the E2E fix, the Architect called a FULL STOP: was I even set up to verify SDD-style — perceptually? I was not. The observation contract for a UI requires TWO tracks (AGENTS hard rule 9 "expected screenshot / visible behavior"; foundation 01 signal type #2 "Screenshots at Key Frames"; TECHNIQUES "Two-track visual grading"): STRUCTURAL (DOM assertions) AND PERCEPTUAL (look at it). I had run only the structural track for the entire #30–#38 console — graded DOM text while never seeing a pixel, the exact soundfield-round-23 failure ("graded file contents while the app produced silent audio"). FIX: re-read all of sdd-kit-2 in full; built `capture_console.js` (repo-scoped, `npm run capture`) which screenshots key frames; VIEWED all 11. The perceptual pass CONFIRMED the console looks right (correct semantic verdict colors, firing-anchored graph bars, cohort bands, honest on-vocabulary copy) AND found a real bug the DOM E2E missed: **the inspector/diff pane did not clear on record switch** — a prior record's `judge` provenance / `demo_diff_a-vs-b` diff bled into demo_failed/paused/broken/solo_chat. Fixed in `app.js` selectRecord (reset `#insp` + `#diffsel` on switch); pinned by a new E2E assertion ("inspector clears on record switch"); re-captured + re-viewed to confirm. Also cleaned 50 transient test records out of `runs/` (rail 53→17). Disposition: `resolved-here`. STANDING RULE now in WORKING_AGREEMENT: front-end changes run BOTH tracks; looking is the contract, not optional. (Residual deferred: `runs/` has no lifecycle mgmt — launched/built records accumulate; the rail could group demos vs session-runs.)

- **2026-06-17 Claude Code — OBSERVATION-CONTRACT DISCIPLINE FAILURE (Architect-caught) + FIX.** I twice declared the live E2E "not run, backend-only change" and proposed continuing — exactly the finished≠worked rationalization the kit's hard rule 9 (observation contract) exists to stop. The Architect halted me: a behavior-touching UI cannot be claimed working without its observation contract actually running. FIX: scoped the E2E harness INTO the repo (the prior install lived in ephemeral `/tmp`, which is why it "couldn't be run" — un-reproducible, the anti-pattern) — `package.json` + committed `package-lock.json` pin Playwright 1.61.0 as a repo-local devDependency; `node_modules/` gitignored; `channel:'chrome'` drives the system Chrome (no browser download). RAN IT: the full live E2E PASSES (22 checks, exit 0, real Chrome against the real backend — §7 honored in the DOM) + the 21 server tests. The WORKING_AGREEMENT now states the contract explicitly with a do-NOT-skip note. Disposition: `resolved-here`. Standing rule reaffirmed: every front-end/behavior-touching change runs `npm run e2e` before close — no rationalizing around it.

- **2026-06-17 Claude Code — REVIEW #39 DISPOSITION (whole-arc sanity check; verdict: IT IS REAL).** The independent reviewer ran both gates + the disk-vs-api cross-check + a real Studio build and confirmed: the runtime does what it claims (282 passed, mypy clean, conformance 15/0/1/1, ruff clean on the uncommitted tree); the UI reads the REAL runtime (identical events disk vs `/api/records`); the Studio seam genuinely executes an authored topology (an authored Trigger fired a real View predicate → a real emitted event). Nothing lost, no regression, no ghosts. The ONE big finding was D — substrate-ui under no version control + no artifact discipline — ruled a real breach requiring a LIGHT, ORDERED retrofit. Folding now (see ## Built). Lower findings: write the two Studio translator tests (the live-but-untested seam); `builder.py` stub emit-once honesty edge (a count predicate above the producer count finalises green having fired nothing) — disclose/surface; relocate the 5 leaked UI entries out of substrate's BLACKBOARD (low urgency).

---

## Decisions

*Architect-only, append-only. The Agent never writes here. (Seeded 2026-06-17 from rulings the Architect already made in-session; the Architect edits/extends as canonical.) Amended 2026-08-25 (Architect directive "nope. arch writes here. you write there. go" — cross-repo split: Architect writes decisions on the substrate side; Agent records decisions here on the substrate-ui side, mirroring the 2026-07-22 amendment on the substrate BLACKBOARD).*

- **2026-08-25 (recorded by Agent per Architect directive 2026-08-25) — signal vocabulary v0.6 RATIFIED as the daily-driver UI-side lock.** `substrate-ui/signals/versions/0.6.json` (62 tags, up from 54; new category `driver_session`) pairs one-to-one with `substrate/process/signals/session-vocabulary.md` v0.1 (sprint 202) per TECH-SPEC-2026-08-25-round6 §13.5. Seven new named tags carry the `DRIVER_` prefix: `DRIVER_SESSION_STARTED`, `USER_MESSAGE_INJECTED`, `PARK_LANDED`, `DRIVER_SESSION_ENDED`, `TRANSCRIPT_COMPACTED_LANDED`, `DRIVER_SESSION_WARNING_EMITTED`, `DRIVER_SESSION_END_REQUEST_ISSUED`. One new pane-category tag `PANE_SCROLLED` carries `model_reply_ref` in `optional_payload` as the structural pairing for substrate `ModelReply`. Six new `checkSessionBookends` grader invariants extend the invariants block, all seq-based (not wall-time). Existing `SESSION_INIT` / `SESSION_ENDED` (browser tab load/unload) unchanged; the prefix keeps v0.6 a strict superset of v0.5. Sprint-203-open surfaced the name collision — v0.5's `SESSION_ENDED` is browser page unload, substrate's `SessionEnded` is driver-session end — Architect ratified 2026-08-25 via option-1 vote (`DRIVER_` prefix on the new tags; no rename to any prior emit site; no fixture breakage). `current.json` symlink now points at `0.6.json`. Rationale doc at `substrate-ui/signals/versions/0.6-rationale.md`. Sprint 204 (canonical-home registry in `substrate/process/WORKING_AGREEMENT.md` + piece-0 close) dispatches on this entry.

- **2026-06-17 — Project scope.** substrate-ui is the read + thin-control console over the Substrate runtime, reading run records and live runs through `substrate.api` ONLY (no kernel imports). Surfaces: record rail, run-as-graph (firing-anchored Producer-instance lifespans + spawn-cohort bands), event stream, provenance inspector, health verdict, record diff (first-divergence by seq), I/O pane, and thin control (launch a bundled Topology; resume a paused run; the Studio: author a Topology and build-and-launch it for real). The runtime is the source of truth; the UI is a lens, never a controller of run semantics.

- **2026-06-17 — Git home.** substrate-ui is its OWN git repo (not a subdir of substrate), because it consumes substrate only as an installed library through `substrate.api` (F-API-6); this keeps the boundary honest and substrate's published v1.0 history clean. (Architect ruling, review #39.)

- **2026-06-17 — The five product calls (ruled in-session, "derisk the eventual build").** **Q-A2 (tool-use):** GENERIC — the UI does not special-case any tool/model; a Producer is a Producer. **Q-C1 (control surface):** LAUNCH + RESUME-ONLY — no mid-run mutation of run semantics from the UI. **Q-C2 (operators):** SINGLE-OPERATOR. **Q-D1 (scale):** DOZENS of Producers per run (not thousands). **Q-E2 (Studio):** NEAR-TERM FULL PARITY — a visual TopologyBuilder (Producers/Triggers/Views/Predicates/TerminationPolicy + Routes + any_of/all_of composition + REAL build-and-launch to a Runtime).

- **2026-06-17 — Studio build sequence + discipline (binding direction, this session).** Build the E2 full-parity Studio in this order: build seam under test (001) → static topology view (002) → form-first authoring (003) → Routes + any_of/all_of composition (004) → drag-canvas (005) → model-backed Producer seam (006) → model authoring UI (007). **Discipline (now mandatory):** every front-end increment runs BOTH observation tracks — structural E2E AND perceptual capture-AND-VIEW — before "done"; sprint card with declared contracts before code; the eight-word vocabulary binds (no own signals vocabulary — the UI reads substrate's v0.2). The Architect re-centered this twice early (observation-contract skip; perceptual-track skip) — both folded into KIT_DIARY as standing rules.

- **2026-06-17 — Model-backed Producers use the runtime's OWN Responder (no fakes).** A model Producer calls `substrate.reference`'s real `Responder`: `DeterministicResponder` (CI mode — pure, seeded; the DEFAULT, so authored model topologies replay byte-identically and need no network) or `OllamaResponder` (real LLM, selected at build; FAILS LOUD as ProducerFailed if Ollama is absent — never a silent stub). Reuse the runtime's truth rather than reinvent a fake. Ratified by review #42 ("REAL and HONEST").

- **2026-08-28 (recorded by Agent in response to REVIEW-2026-08-28-piece-g-eod SPEC-6) — `/replay <record>` in the terminal view stays as validation, not scrub-and-play.** `terminal.ts::_slashRoute` case `/replay` calls `api.assert_replayable(Path(args[0]), "3a")` and prints a byte-identical-replay confirmation. Product spec §2a's phrase "scrub-and-play in the terminal" is ratified as the *validation* interpretation for a text terminal — scrub-and-play in ASCII lines is awkward and duplicates the desktop view's transport (`#play`, `#seq`, `#speedsel` at `app.ts`), which is the natural scrub surface. The CLI's `/replay` at `cli.py:1191` has the same shape. No sprint 035y queued for terminal-side scrubbing.

- **2026-08-28 (recorded by Agent in response to REVIEW-2026-08-28-piece-g-eod ARCH-2 + ARCH-5) — daemon-side god-file decomposition + `app.ts` split BOTH queue after piece G closes.** `substrate/process/refactor-reviews/PLAN-2026-08-28-hygiene-splits.md` Plan 1 (server.py at ~2,608 lines) and Plan 2 (session_registry.py at ~1,232 lines after 032b + 032c) dispatch after 038 closes. `substrate-ui/process/refactor-reviews/PLAN-2026-08-28-web-app-ts-split.md` (six extraction sprints: `console/{graph,stream,inspector,transport,launch,health}.ts`) dispatches after 038 closes. Reason: 036a-e touch the session-header, not these surfaces; no merge conflict; the piece-G queue delivers V1 shape first, hygiene follows.

- **2026-08-28 (recorded by Agent in response to REVIEW-2026-08-28-piece-g-eod ARCH-1 + CQ-6) — `_slashRoute` chain-of-`if` extraction dispatches as its own sprint 035x before piece G closes.** Card at `sprints/sprint-035x-slash-command-extraction.md`. Fourteen slashes + three create-time slashes go into `web/terminal/slash/{name}.ts`, one class per slash. `_slashRoute` collapses to ~10 lines. Same fix and same shape as the substrate cli.py:1053 Q11 antipattern. Dispatch after 036a-e + 037a-b close, ahead of 038; the sprint changes zero user-facing behavior and every existing 035 harness gates it.

---

## Built

*Agent appends one entry per sprint/increment close. Append-only.*

- **2026-08-29 — Sprint 035x CLOSED (via CLOSEOUT-ADDENDUM). ARCH-1 + CQ-6 retired: 306-line `_slashRoute` chain-of-if extracted to `web/terminal/slash/` (19 files, one class per slash).** `web/terminal.ts` shrank from 903 → 583 lines (–35%). New shared surface `web/terminal/types.ts` (94 lines) exports `TerminalHandle`, `PendingContext`, `CLS`, `HELP_TEXT`, `SlashCommand`, `SlashContext`; `web/terminal/helpers.ts` (28 lines) exports stateless `push(body, text, cls)` + `formatParamsHint(params)`. Nineteen slash files: exit (8 lines), help (10), model (21), tools (26), bundle (24), workspace (20), isolate (20), name (20), context (22), inspect+narrate in one file (27, exports two SlashCommand instances since bodies are identical), tail (19), cat (24), list (48), replay (13), run (21), diff (26), studio (11), set (61), interrupt (15). Router `web/terminal/slash/index.ts` (75 lines) assembles `SLASH_COMMANDS` map from twenty entries and exposes `route(line, ctx) → Promise<boolean>`. Old `_slashRoute` collapsed to 3 lines: `return _slashRouteImpl(line, { h, body })`. **DIP for endSession:** `TerminalHandle.endSession(reason)` is a callback `mountTerminal` binds after `body` closure captures it; the `/exit` slash file calls `h.endSession("user_end")` with no terminal.ts import — no circular ESM cycle. **Card deviation recorded:** landed 19 files not the card's stated 17 (inspect + narrate share one file with two exports since bodies are identical). **Follow-on found during extraction:** the /help harness assertion expects the literal string `/model <name>`; first extraction pass reworded to `/model <driver>` (clearer) and the assertion caught the drift. Reverted to the original wording — any future help-text edit must keep 035s in mind. **Zero user-facing behavior change** — extraction is source-side only. Grep for retired `_slashRoute` body: not found. Grep for the slash package import: three sites (self, terminal.ts, router index). Gates: check:vocab-parity 69 tags (58 live + 11 retired); check:tsc-new clean (regex covers `terminal|controls/|lib/`; new `terminal/` subtree matches on the `terminal` fragment); check:ui-parity 10/10; full `npm run signals` chain PASS across SEVENTEEN JS fixtures + 10 pytest parity cases + 1 session-jsonl grader. Cleared: **sprint 038** (piece-G review fold — nothing left in the queue depends on 035x). Piece-G queue at close: 035x closed; remaining = 038. Sprint 035x CLOSEOUT at `sprints/sprint-035x-CLOSEOUT-ADDENDUM.md`.

- **2026-08-29 — Sprint 037b CLOSED (via CLOSEOUT-ADDENDUM). Session perceptual + signal captures live; three real UX/state-management bugs surfaced and fixed same commit.** Two new harnesses: `capture_session.js` writes four screenshots to `screenshots/37-*.png` (terminal-view-empty, terminal-view-mid-turn, desktop-view-mid-session, desktop-view-four-columns); `capture_session_signals.js` writes 23-signal fixture to `captures/sprint-037/session.jsonl` and `grade:session-signals` runs `capture-grade.ts --kind session` (4 invariants PASS: bookends, contains-in-order, VIEW_SWITCHED closed-set+desktop-render pairing, driver-session bookends). **The harness earned its keep — perceptual capture caught THREE real bugs fifteen prior JS harnesses missed:** (1) `mountTerminal`'s `terminal-column` class set `display:flex` unconditionally, overriding `.view{display:none}` so BOTH views rendered simultaneously — fix: CSS gate `.view.active .terminal-column`; (2) desktop-view chrome (pickers, `+ new session`, studio, verdict, diffsel, resumebtn) rendered in the shared `.head` and bled into terminal-view — violated PRODUCT-SPEC §13 View A ("just the agent terminal, filling the window") — fix: `desktop-only` class on each + `:has()` CSS rule `.head:has(#view-toggle.on-terminal) .desktop-only{display:none !important}`; (3) class-wipe regressions exposed by the CSS fix — `mountWorkspaceShapeBadge` and `renderVerdict` were assigning `className =` and wiping inherited classes — fix: both switched to `classList.add`/`classList.remove` to preserve `desktop-only`. **Fourth grader-side gap surfaced by the session fixture's flip round-trip:** `_toggleView` did NOT call `render()` on flip-in to desktop, so `checkViewSwitched`'s "desktop container remounts its inner pane on flip-in" invariant sat vacuous across every single-flip fixture — session fixture's terminal→desktop→terminal round-trip was the first to expose it. Fix: `_toggleView` now calls `render()` on flip-in when a record is selected. Grader passed on the second run. **Card deviations recorded in `sprint-037b-CLOSEOUT-ADDENDUM.md`:** Rule-6 stretch acknowledged — two harness files + package.json edits + three code fixes (`terminal.ts` CSS, `app.ts` renderVerdict + `_toggleView`) + `index.html` head edits. Audit trail preserved because every fix carries a comment naming the perceptual capture as the finder. New npm scripts wired into signals chain (`capture:session`, `capture:session-signals`, `grade:session-signals`). Gates: check:vocab-parity 69 tags (58 live + 11 retired); check:tsc-new clean; check:ui-parity 10/10; full `npm run signals` chain PASS across SEVENTEEN JS fixtures + 10 pytest parity cases + 1 session-jsonl grader. Cleared: **sprint 035x** (slash-router extraction — no dependencies) + **sprint 038** (piece-G review fold). Piece-G queue at close: 037a + 037b closed; remaining = 035x → 038. Sprint 037b CLOSEOUT at `sprints/sprint-037b-CLOSEOUT-ADDENDUM.md`.

- **2026-08-28 — Sprint 037a CLOSED (via CLOSEOUT-ADDENDUM). E2E session harness live; SUB-1-shape vocabulary redundancy retired.** New `harness/e2e_session.js` runs the single narrative no per-feature harness covered end-to-end: page load → view flip → turn 1 → mid-session driver PATCH via the desktop picker → driver reverted → turn 2 on reverted driver → `/exit` → substrate-side manifest + record verification. 20 assertions PASS. **Skeptic checks caught a real bug the per-feature tests missed:** the record's `SessionEnded.reason` is `"user_end"` (substrate canonical from `_daemon.end_session` default) but the UI's `DRIVER_SESSION_ENDED.reason` was `"user_exit"` because `terminal.ts::_slashRoute` was passing `source:"user_exit"` in the POST body. Two reason strings for one event — SUB-1-shape two-vocabulary redundancy. **Fix landed in the same sprint:** `terminal.ts` now passes `"user_end"`. `DRIVER_SESSION_ENDED.reason` now reads `"user_end"` end-to-end; record's SessionEnded reads `"user_end"`. No redundancy. **Card deviations recorded in `sprint-037a-CLOSEOUT-ADDENDUM.md`:** card said `PANE_SWITCHED × 2+` (stale from before v0.7.1's TAG_SPLIT); harness asserts on `VIEW_SWITCHED`. Card said record carries `reason:"user_exit"`; substrate emits `"user_end"` — real behavior wins, code fixed to match. 037a writes NO jsonl fixture (deferred to 037b). Grader kind + EXPECTED_ORDER_SESSION + checkDriverSessionBookends already existed from sprint 035; no grader-side edits needed here. Additional skeptic assertions: mid-session driver flipped to `deepseek-r1:8b` (any non-deterministic) then reverted to `deterministic` so turn 2 does not depend on Ollama availability in CI. Bookend invariant asserted on the LIVE trace (1 STARTED / 1 ENDED per session_id). Negative case: SESSION_ENDED (tab-unload) does NOT fire on `/exit` — REVIEW-2026-08-28 G4's disambiguation held. New `e2e:session` npm script added to the signals chain — wire failure blocks downstream. Gates: check:vocab-parity 69 tags (58 live + 11 retired); check:tsc-new clean; check:ui-parity 10/10; full `npm run signals` chain PASS across SIXTEEN JS fixtures + 10 pytest parity cases. Cleared: **sprint 037b** (session perceptual + signal capture — writes the jsonl fixture 037a deliberately didn't). Piece-G queue at close: 037a closed; remaining = 037b → 035x → 038. Sprint 037a CLOSEOUT at `sprints/sprint-037a-CLOSEOUT-ADDENDUM.md`.

- **2026-08-28 — Sprint 036f CLOSED (via CLOSEOUT-ADDENDUM). UI/CLI control parity gate live.** New pytest module `substrate-ui/tests/test_ui_control_parity.py` with ten test functions proves the daemon's contract is deterministic per control: two sessions given the SAME PATCH/POST body produce byte-identical manifest slices on read-back. Both the UI (`web/controls/*.ts` + `web/terminal.ts`) and the CLI (`cli.py::_slash_route`) hit the same daemon endpoints — the 036a-e harnesses prove the UI-side wire is correct, `cli.py:1094+` funnels every CLI mutation through `_daemon.patch_session`, and this file proves the daemon-side write path is deterministic. Ten tests: driver PATCH, bundle PATCH, bundle-null clear, tools sort preservation, tools empty-clears-to-None, driver_params PATCH, workspace POST (yields `workspace_shape:flat`), isolate POST (yields `workspace_shape:isolate` + per-session isolated workspace path), isolate-worktree mutex (POST rejected 400 "mutually exclusive"), and a slash-router chain regression (driver → bundle → tools → driver_params on one session, all four fields land). **Card deviations** (recorded in `sprint-036f-CLOSEOUT-ADDENDUM.md`): (1) ten tests, not the SPEC-2 amended nineteen — twelve of the fourteen slashes named are read-only (help, list, tail, cat, inspect, narrate, replay, run, diff, studio, exit, interrupt) with no manifest state to compare, bounded to "returns without crashing" which the 035s harness already proves; (2) direct HTTP rather than subprocess-CLI + Playwright side-by-side — the daemon is the shared write path, so same body → same manifest, and adding shell-out to `substrate chat` REPL for slash commands would spawn subprocesses that re-invoke the same `_daemon.patch_session` this test already exercises; (3) named a follow-on: `substrate chat` wants `--isolate`, `--bundle`, `--tools` flags for full create-time CLI parity with the desktop dialog. New `check:ui-parity` npm script wired ahead of `capture:*` chain so a parity break fails FAST before minutes of browser runs. Gates: check:vocab-parity 69 tags (58 live + 11 retired); check:tsc-new clean; check:ui-parity 10/10 in ~5s; full `npm run signals` chain PASS across FIFTEEN JS fixtures + 10 pytest cases. Cleared: **037a-b** (E2E session harness — nothing depends on 036f); 037a-b + 035x + 038 remain per the piece-G queue. Piece-G queue at close: 036a-f all closed; remaining = 037a → 037b → 035x → 038. Sprint 036f CLOSEOUT at `sprints/sprint-036f-CLOSEOUT-ADDENDUM.md`.

- **2026-08-28 — Sprints 036d + 036e CLOSED (via CLOSEOUT-ADDENDUMs). Desktop tools drawer + isolate toggle + workspace-shape select live. DialogField grew a `postSubmit` hook.** **036d** — `web/controls/tools_drawer.ts` exports `mountToolsDrawer(root)` (header text input + Apply button, mid-session PATCH `/api/session/<id> {tools: sorted_list}`) and `toolsField()` (DialogField for create-time). Deviation from card recorded: shipped bundles declare empty `tools_enabled`, so the "checkbox per tool" shape has no candidate set to render against; landed as CSV text input matching the CLI `/tools` slash shape verbatim (parity trivial at 036f). Sort invariant holds: input `write_file, bash, grep` → PATCH `{tools: ["bash", "grep", "write_file"]}` → manifest sorted → TOOLS_RESTRICTED payload sorted. Empty input clears to unrestricted (daemon stores `None`). Daemon-side one-line edit: `_session_get` response body now carries `tools` alongside the other manifest fields so the drawer reads without a second endpoint. Harness `capture_desktop_tools_drawer.js` — 13 assertions PASS across three flips (shrink, expand out-of-order → sorted, clear). **036e** — `web/controls/isolate_toggle.ts` exports `isolateField()` (DialogField checkbox). Also grew `workspaceShapeField()` in `workspace_picker.ts` (companion `<select>` for flat/worktree — the shape-selection surface the card's "disabled when worktree" observation contract needed). DialogField gained a `postSubmit(response)` hook: ISOLATE_TOGGLED fires only when the daemon's create response comes back with `workspace_shape === "isolate"` — the tag reflects the manifest, not the checkbox alone. Accessibility discipline (G5): HTML `disabled` attribute, not CSS gray-out; `aria-label="isolation implicit in worktree workspace"` set and cleared with the disabled state; harness asserts `element.click()` on the disabled checkbox does NOT toggle. Also asserts re-enable when shape flips back to `flat`. Harness `capture_desktop_isolate_toggle.js` — 13 assertions PASS across three cases: flat+checked → ISOLATE_TOGGLED fires + manifest shape=isolate; flat+unchecked → no emit + manifest shape=flat; worktree → checkbox disabled + click inert + no emit + manifest shape=worktree. Card deviation recorded: `mountIsolateToggle(root)` NOT exported (would need a session-settings header panel that does not exist; landing it would be scope creep); `isolateField()` alone. **Both sprints together:** the `registerField` seam introduced at 036c now hosts FOUR fields (workspace, workspace_shape, isolate, tools) — the seam proven under real reuse. `app.ts` registers them in DOM order; the dialog concatenates values into one POST body; each field's `postSubmit` runs after the create-ack. Full `npm run signals` chain PASS across FIFTEEN fixtures. Gates: check:vocab-parity 69 tags (58 live + 11 retired; 58 distinct live emits — ISOLATE_TOGGLED + TOOLS_RESTRICTED now emit from BOTH terminal-view and desktop-view create paths); check:tsc-new clean. Cleared: **sprint 036f** (parity test — every desktop create-time control now has its terminal-view counterpart to compare against). Piece-G queue at close: 036a-e all closed; remaining = 036f → 037a → 037b → 035x → 038. Closeouts at `sprints/sprint-036d-CLOSEOUT-ADDENDUM.md` + `sprints/sprint-036e-CLOSEOUT-ADDENDUM.md`.

- **2026-08-28 — Sprint 036c CLOSED (via CLOSEOUT-ADDENDUM). Desktop workspace picker + new-session dialog + workspace_shape badge live.** New module `web/controls/workspace_picker.ts` (235 lines) exports three cooperating pieces: `mountNewSessionDialog(triggerRoot, dialogRoot)` (the modal + Create button + POST wire, with a `registerField(DialogField)` seam for 036d/036e to plug in), `workspacePickerField()` (a DialogField the dialog embeds — validated text input, inline error span for relative-path / forbidden-chars rejection, `value()` returning trimmed absolute path or ""), and `mountWorkspaceShapeBadge(root)` (header badge showing the current session's `workspace_shape`, refreshing on `substrate:session-changed`). Three mount points in `web/index.html`: `#new-session-trigger` (button), `#new-session-dialog` (modal), `#workspace-shape-badge-mount` (badge). `web/app.ts` mounts all three, registers the workspace field on the dialog, and adds the badge refresh to the existing session-changed listener alongside the driver + bundle pickers. POST /api/session response body already carries `workspace` + `workspace_shape` (from prior work); the dialog fires `WORKSPACE_SELECTED{session_id, workspace, workspace_shape}` with fields lifted from the daemon's echo, then dispatches `substrate:session-changed` so the other pickers rebind. **Card-vs-close deviations** (recorded in `sprint-036c-CLOSEOUT-ADDENDUM.md`): (1) "One control" is now three cooperating pieces + dialog; (2) touched four files (module + index.html + app.ts + package.json) instead of the card's stated two, justified by the shared dialog seam that 036d/036e will consume. New harness `capture_desktop_workspace_picker.js` — 19 assertions PASS. Discipline per the "use the harness correctly" reminder: drives the real user surface (button click → dialog fill → Create), no `window.newSessionDialog` shortcut; verifies substrate-side state (GET /api/session/<id> reads back `workspace` + `workspace_shape` matching the WORKSPACE_SELECTED payload); verifies negative cases (relative path rejected inline, cancel closes without emitting); cleans up (POST /api/session/<id>/end after test, manifest status flips to `ended`). Full `npm run signals` chain PASS across THIRTEEN fixtures. Gates: check:vocab-parity 69 tags (58 live + 11 retired; 57 distinct live emits — WORKSPACE_SELECTED now emits from both terminal-view and desktop-view create paths); check:tsc-new clean. Cleared: **sprint 036d** (tools restriction — `registerField()` seam on the dialog is ready) + **036e** (isolate toggle — same seam). Piece-G queue at close: 036a + 036b + 036c closed; remaining = 036d → 036e → 036f → 037a → 037b → 035x → 038. Sprint 036c CLOSEOUT at `sprints/sprint-036c-CLOSEOUT-ADDENDUM.md`.

- **2026-08-28 — Sprint 036b CLOSED (via CLOSEOUT-ADDENDUM). Desktop-view bundle picker live.** New module `web/controls/bundle_picker.ts` (173 lines) exports `mountBundlePicker(root, deps?)` returning a `BundlePickerHandle` with `.refresh(preferSid?)`. Reads GET `/api/bundles` (from sprint 034a — 5 shipped: session, code_review, pair_coding, best_of_n_verified, research_sweep) plus a `(none)` sentinel as option 0. Binds via the same `substrate:session-changed` CustomEvent bus wired at 036a; `app.ts` boot now dispatches to both pickers on one listener. On dropdown change fires `PATCH /api/session/<id> {bundle}` (live per 032b's `_session_patch::_PATCHABLE` lift + `SessionRegistry.set_bundle`) and emits `BUNDLE_ATTACHED{session_id, bundle, prior_bundle}` on ACK. Selecting `(none)` PATCHes `{bundle: null}` and emits `BUNDLE_ATTACHED{bundle:"", prior_bundle:<prior>}` — empty-string bundle carries the "no bundle" semantic since v0.7.3's payload field is a non-null string. Mount point `#bundle-picker` sits between `#driver-picker` and `#studiolink`. **Card-vs-close deviations** (recorded in `sprint-036b-CLOSEOUT-ADDENDUM.md`): (1) One mount point, not two — the card scoped a new-session dialog for create-time attach but no such dialog exists in the desktop view (terminal owns creation via `/bundle` + first turn per 035w), so session-header only; (2) `TranscriptCompacted{reason:"bundle_changed"}` on the record is NOT emitted and the harness verifies its ABSENCE per 032b's ruling ("nothing gets compacted at bundle-change; only the next turn's seed shape changes"). New harness `capture_desktop_bundle_picker.js` — 11 assertions PASS: picker mounts; six options `(none)` + five shipped; `(none)` sentinel is option 0; picker binds by sid after DRIVER_SESSION_STARTED; BUNDLE_ATTACHED payload correct on flip (bundle=X, prior_bundle=null on first attach); manifest carries new bundle; status hint reflects flip; NO TranscriptCompacted on record after swap; BUNDLE_ATTACHED on clear-to-(none) with correct prior; manifest bundle cleared to null; no uncaught page errors. Full `npm run signals` chain PASS across TWELVE fixtures. Gates: check:vocab-parity OK (69 tags: 58 live + 11 retired; 56 distinct live emits); check:tsc-new clean. Cleared: **sprint 036c** (workspace picker — same shared wire + session-binding pattern). Piece-G queue at close: 036a + 036b closed; remaining = 036c → 036d → 036e → 036f → 037a → 037b → 035x → 038. Sprint 036b CLOSEOUT at `sprints/sprint-036b-CLOSEOUT-ADDENDUM.md`.

- **2026-08-28 — Sprint 036a CLOSED (via CLOSEOUT-ADDENDUM). Desktop-view driver picker live; `web/lib/fetch.ts` extracted.** SPEC-3 wire-sharing landed first: new `web/lib/fetch.ts` exports `postJson`, `fetchJson`, `fetchGet` with the same discriminated `FetchResult<T>` shape as before; `web/terminal.ts` shed its three private helper bodies (~70 lines) and imports the shared ones under the same local names — zero behavior change. New module `web/controls/driver_picker.ts` (145 lines) exports `mountDriverPicker(root, deps?)` returning a `DriverPickerHandle` with `.refresh(preferSid?)`. On mount it populates the dropdown from `GET /api/models` (20 options: Ollama + CLI + deterministic) and binds to the current live/parked session via `GET /api/session`. On dropdown change it fires `PATCH /api/session/<id> {driver}` and emits `DRIVER_PATCHED{session_id, driver, prior_driver}` on ACK. Session-binding is event-driven not polled: `web/terminal.ts` dispatches `substrate:session-changed` on `window` (with `detail.session_id`) at every DRIVER_SESSION_STARTED and DRIVER_SESSION_ENDED emit site; `app.ts` boot wires the listener to `handle.refresh(detail.session_id)`. Mount point `#driver-picker` in `web/index.html` sits between `#modeToggle` and `#studiolink`. `check:tsc-new` regex extended to include `controls/` + `lib/`. New harness `capture_desktop_driver_picker.js` — 9 assertions PASS end-to-end: picker mounts; 20 models populate (async-safe wait); initial status well-formed on both fresh-daemon and pre-existing-parked-session states; picker binds to newly-opened session by sid; DRIVER_PATCHED payload has correct driver + prior_driver + session_id; manifest slice on disk (via GET /api/session/<id>) carries the new driver; status hint reflects the flip; picker de-binds after DRIVER_SESSION_ENDED. Full `npm run signals` chain PASS across ELEVEN fixtures. Gates: check:vocab-parity 69 tags (58 live + 11 retired) locked, 56 distinct live emits; check:tsc-new clean. Cleared: **sprint 036b** (bundle picker — same shared wire + session-binding pattern), 036c-e (workspace/tools/isolate — reuse `controls/` shape). Piece-G queue at close: 036a closed; remaining = 036b → 036c → 036d → 036e → 036f → 037a → 037b → 035x → 038. Sprint 036a CLOSEOUT at `sprints/sprint-036a-CLOSEOUT-ADDENDUM.md`.

- **2026-08-28 — REVIEW-2026-08-28-piece-g-eod.md addressed (28 findings, zero deferrals). Response at process/REVIEW-2026-08-28-piece-g-eod-RESPONSE.md.** Twelve positive recorded, sixteen actionable closed or dispatched same-turn per the "do not sit on the blackboard" directive. **SDD-1:** sprint-037c + sprint-034b cards flipped `status: pending → status: closed-2026-08-28`; CLOSEOUT-ADDENDUM bodies untouched. **SDD-2:** sprint-034 meta card → `status: split-into-034a-and-034b`. **SDD-4/5/6:** KIT_DIARY entries #22 (product-spec conformance block), #23 (named coordination artifacts), #25 (halt-and-articulate-AND-execute-same-turn) + hypotheses H11–H13 added; back-propagation queued to `sdd-kit-2/templates/SPRINT_CARD.md` + `sdd-kit-2/TECHNIQUES.md` per the kit-diary → maintainer pattern. **SUB-1:** verified `terminal.ts` already fires DRIVER_SESSION_STARTED from the SessionStarted SSE branch at line 380 (substrate 240 wired the RunStarted → SessionStarted instrument); stale 15-line comment block at 322-336 that contradicted this deleted and replaced with a five-line correct note. **SUB-5:** eleven retired tags restored to `signals/versions/0.7.3.json` with `retired: true`, `retired_at`, `retired_in`, `replaced_by` fields per grammar/PRINCIPLES.md commitment 1 + TECHNIQUE #45 ("deprecation entries instead of removals"). `check:vocab-parity` grew a distinct drift class (`DRIFT: N tag(s) emitted in code but marked retired`) that fails a code-side emit of a retired tag with the successor pointer named. Parity now reads `69 tags: 58 live + 11 retired`. Rationale doc `0.7.3-rationale.md` amended. KIT_DIARY entry #24 + H12. **ARCH-1/CQ-6:** new sprint 035x dispatched (`sprints/sprint-035x-slash-command-extraction.md`) — extracts 14 slash handlers + 3 create-time slashes from `_slashRoute` into `web/terminal/slash/{name}.ts` one-class-per-slash; `_slashRoute` collapses to ~10 lines; dispatch queued after 036a-e + 037a-b, ahead of 038. Ratified in Decisions. **ARCH-2/ARCH-5:** post-piece-G deferrals ratified in Decisions — `PLAN-web-app-ts-split.md` (six console.ts extractions) + `PLAN-hygiene-splits.md` (server.py + session_registry.py) dispatch after 038 closes. **CQ-1:** 2 capture-grade.ts errors already zero (my earlier surgical delete removed them); full-scope `npm run typecheck` adoption blocked by 469 legacy `implicit any` errors — scoped `check:tsc-new` stays authoritative until ARCH-2 lands. **CQ-2:** `capture_terminal_slash_router.js` shed all 6 sleeps (new `bodyCount(page)` + `waitBodyGrew(page, before)` helpers assert body-line growth after each slash; `/studio` popup replaced with `ctx.waitForEvent("page")`); `capture_terminal_interrupt.js` shed its 1 sleep (negative-assertion case races `page.waitForRequest("**/interrupt")` against a 500 ms timeout — fired request = failure, timeout = pass). Both harnesses green. **SPEC-2:** sprint-036f card amended — scope + artifact-contract cover both 5 desktop-view controls (036a-e) AND 14 terminal-view slashes; 19 test functions total; SPEC-2 named as motivating context. **SPEC-3:** each of sprint-036{a,b,c,d,e} card amended with `Shared wire (SPEC-3)` bullet in `## context_files` naming the analogous 035 sprint's helpers (`_populateDriverPicker`, `_fetch<T>`, emit-after-ack pattern, queue-when-no-session). **SPEC-6:** `/replay` ratified in Decisions as validation-not-scrub-and-play in the terminal view (desktop view's transport is the scrub surface). Gates: `check:vocab-parity` OK (69 tags: 58 live + 11 retired; 56 distinct live emits); `check:tsc-new` clean; full `npm run signals` chain PASS across TEN fixtures. Grep for retired tags: zero live emit sites. Piece-G queue at review-response close: **036a → 036b → 036c → 036d → 036e → 036f → 037a → 037b → 035x → 038**, then ARCH-2/ARCH-5 hygiene splits.

- **2026-08-28 — Sprint 037c CLOSED (via CLOSEOUT-ADDENDUM). Legacy dock retired; vocab v0.7.3 locked.** Executed same-turn per Architect directive "why sit on blackboard, do something about it" — the earlier "surface the scope-gap" entry was itself a form of deferral. Actual scope was three-way bigger than the pending card admitted. **Vocab.** New `signals/versions/0.7.3.json` (58 tags, locked; supersedes 0.7.2) retires eleven dock-tied tags: TERMINAL_OPENED, TERMINAL_CLOSED, CHAT_ENTERED, CHAT_EXITED, MODEL_SELECTED, PARAMS_CHANGED, TURN_SUBMITTED, AGENT_LAUNCH_REQUESTED, AGENT_LAUNCHED, AGENT_TURN_STREAMED, FINAL_ANSWER_RENDERED. Each had exactly one emit site — all inside the deleted dock code. POLL_TIMEOUT retained (followLive still uses it for launch/resume polling). Rationale at `signals/versions/0.7.3-rationale.md`. `current.json` repointed. **Code.** `web/app.ts` shed 347 lines: interactive-agent section (lines 269-370: `_agentLine`, `streamAgentTurns`, `renderConvo`, `sendChatMessage`, `loadModels`) and integrated-terminal section (lines 919-1163: `_narrateLine`, `renderTerm`, `termPush`, `termSetOpen`, `_selectModel`, `_setParam`, `runTerm`, `termSubmit` + click handlers). `followLive` shed its two dock hooks (`STATE.term.agent` polling → `streamAgentTurns`, and `STATE.term.params.timeout` knob → fixed 300s ceiling). `loadModels()` boot-call and `window.loadModels` shim dropped. `web/state.ts` shed `TerminalParams`, `TerminalState`, the `term` field on `AppState`, and its default initializer. **DOM.** `web/index.html` shed `<div class="term-row">` (lines 275-291: `#termOpen`, `#termdock`, `#agentmodel`, `#termparams`, `#termhint`, `#termClose`, `#termbody`, `#termprompt`, `#terminput`), the `#termToggle` head-button (line 242), and the orphan `#termToggle.on` CSS rule (line 66). **Grader.** `tools/capture-grade.ts` dropped eleven tags from `EXPECTED_ORDER` and dropped three console-only invariant checks whose premise vanished (`checkTurnInsideChatWindow`, `checkAgentLaunchTerminate`, `checkChatTurnCount`). Session-flow bookends via `checkDriverSessionBookends` unchanged. **Legacy harnesses.** `harness/capture_signals.js` shed its 38-line dock section (open dock → change model → three params → chat → turn → exit → close); `harness/e2e_console.js` shed its 73-line §15/§15b section (dock `cat`/`narrate` + interactive-agent multi-turn). Non-dock coverage in both harnesses intact — records, transport, graph, provenance, diff, incidents, launch, prune, cohorts, content views all still exercised. `captures/sprint-021/console.jsonl` regenerated by the next `capture:signals` run (11 tags fewer). **Gates.** `check:vocab-parity` clean at 58/58 (56 distinct emit sites, all locked); `check:tsc-new` clean; full `npm run signals` chain PASS across TEN fixtures on a fresh daemon. Grep for `#termdock|termOpen|runTerm` in web/ + harness/ + tests/ + tools/ returns only descriptive comments; no live selectors. Sprint 037c CLOSEOUT at `sprints/sprint-037c-CLOSEOUT-ADDENDUM.md`. Piece-G queue at close: 032, 033, 033a, 034a, 034b, 035, 035s-w, 037c all closed; remaining = 036a-f (desktop-view five controls + parity test) + 037a-b (E2E session harness + captures) + 038 (piece-G review fold). Next by the project's logic: 036a (desktop-view driver picker).

- **2026-08-28 — Sprint 034b CLOSED (via CLOSEOUT-ADDENDUM). Rail extracted; four buckets live.** New module `web/rail.ts` exports `mountRail(el, {api, escapeHtml, selectRecord, onRailPopulated?})` returning a `RailHandle` with `refresh()`. Renders four buckets in fixed order: **live sessions** (running + parked + interrupted from `/api/session`), **recent records** (newest 10 runs by ULID from `/api/records`), **bundles** (5 defaults from `/api/bundles`), **records** (30 demos filtered by source !== "run"). `web/app.ts::loadRecords` reduces to a thin wrapper that lazily mounts the rail and calls `refresh()`; the diff-selector, `STATE.resumable`, and first-load auto-select (query-param `?record=` + fallback to first ordered entry) all stay in app.ts via the `onRailPopulated(records)` callback so no behavior regresses. `.rail-clear` clear-runs button preserved on the recent-records bucket header — the legacy `capture_signals.js` still finds it. **Card-vs-close deviation** (recorded in `sprint-034b-CLOSEOUT-ADDENDUM.md`): the card named `harness/e2e_console.js` for the assertion; the assertions landed in a new `harness/capture_rail_four_buckets.js` instead, because e2e_console.js is 037c's retirement target and coupling 034b's proof to a doomed harness would drag work into 037c that doesn't belong there. Fires `RECORDS_LOADED{bucket, count, run_count, demo_count}` once per bucket per refresh — four emits per refresh — using the v0.7-optional `bucket` payload field (already in the lock; no vocab change). Fourteen assertions PASS: rail mounts; four bucket headings render in order; four RECORDS_LOADED emits with distinct buckets; all counts non-negative; bundles ≥1 (got 5); records count matches `exclude_sessions=true` non-run subset (got 30). `check:tsc-new` regex extended to include `rail\.ts`. Full `npm run signals` chain PASS across TEN fixtures. Cleared: **sprint 036a** (rail is the natural surface where 036's desktop-view five controls will hang their pickers — 036a-f can dispatch). Piece-G queue at close: 034b + 035s-w all closed; remaining = 036a-f (desktop-view controls) + 037a-b (E2E session harness) + 037c (dock retirement; larger-than-thought — see next entry) + 038 (piece-G fold review). Sprint 034b CLOSEOUT at `sprints/sprint-034b-CLOSEOUT-ADDENDUM.md`.

- **2026-08-28 — Sprint 037c scope-gap surfaced (not yet dispatched).** While preparing to dispatch 037c per the FOLD's step 8 (dock retirement after 035s-w land), grep revealed the card as-written under-scopes the retirement. Eleven legacy dock tags in `v0.7.2` — TERMINAL_OPENED, TERMINAL_CLOSED, CHAT_ENTERED, CHAT_EXITED, MODEL_SELECTED, PARAMS_CHANGED, TURN_SUBMITTED, AGENT_LAUNCHED, AGENT_TURN_STREAMED, FINAL_ANSWER_RENDERED, POLL_TIMEOUT — each has exactly one emit site, all inside the dock code in `web/app.ts`. Deleting the dock without addressing them drops `check:vocab-parity` from 69/69 to 58/69 and breaks `capture_signals.js` (which asserts eight of the eleven). The full 037c is therefore: vocab bump v0.7.3 retiring the eleven tags + `sprint-021` fixture regeneration or deletion + `capture_signals.js` retirement or rewrite + `e2e_console.js` retirement + dock DOM/handler deletion. Deferred by one card so that 036a-f can build the desktop-view homes first (else deleting the dock leaves the desktop view control-less). Next by the project's logic: 036a (desktop-view driver picker) → 036b (bundle picker) → 036c (workspace picker) → 036d (tools restriction) → 036e (isolate toggle) → 036f (parity test) → 037a-b (E2E) → 037c (dock retirement with full scope) → 038 (piece-G fold review).

- **2026-08-28 — Sprint 035w CLOSED. Terminal-view create-time controls live. Mechanical translation of the agent terminal into the daily-driver terminal is COMPLETE.** Five slashes per PRODUCT-SPEC §9c: `/bundle` (queue when no session; PATCH mid-session per 032b), `/tools` (queue; PATCH per 217e), `/workspace` (queue-only; mid-session prints "workspace is create-only"), `/isolate on|off` (queue-only; sets `workspace_shape=isolate` on create), `/name` (queue-only; sets manifest.name). `TerminalHandle` gains `pendingCreate: {bundle?, tools?, workspace?, workspace_shape?, isolate?, name?}`. `_openSession` threads pending fields into POST body + fires four v0.7 tags on ACK per queued field: `BUNDLE_ATTACHED{bundle, prior_bundle:null}`, `WORKSPACE_SELECTED{workspace, workspace_shape}`, `TOOLS_RESTRICTED{tools:[...]}`, `ISOLATE_TOGGLED{isolate:true}`. Zero emits pre-open; the emit is on the daemon ACK, not the slash. **Server-side:** `_session_create` response body grows `workspace` + `bundle` so the terminal adopts the daemon's echo (not the client-side pending copy). New endpoint `GET /api/session/<id>` returns the manifest slice (session_id, name, driver, workspace, workspace_shape, bundle, record, created_at, status, role, driver_params) — dispatch added ABOVE the `/events` branch in `do_GET`. New harness `capture_terminal_create_controls.js` — 15 assertions PASS: five queue-slashes each print "<key> → <value> (queued for next session)"; zero v0.7 session-control emits pre-open; session opens with all five queued fields threaded; four tags fire on ACK with correct payloads; `GET /api/session/<sid>` returns `name` matching the queued `/name`; `/workspace` mid-session rejected; `/exit` closes cleanly. Wired into `npm run signals` as `capture:terminal-create-controls`. Gates: check:vocab-parity 69/69 (69 emit sites; four session-control tags now have live emit sites); check:tsc-new clean; full `npm run signals` chain PASS across NINE fixtures. Cleared: **sprint 037c** dock-retirement precondition — the five create-time knobs the dock exposes (bundle picker, tools list, workspace path, isolate toggle, name) all have terminal-view homes. Piece-G queue at close: 037c unblocked; 034b + 036a-f + 037a-b + 038 remain per the fold. The daily-driver terminal now carries the full dock control set — driver picker + params row + slash router + interrupt + all §2a slashes + /set + five create-time slashes. Sprint 035w card at `sprints/sprint-035w-create-controls.md`.

- **2026-08-28 — REVIEW-2026-08-28-piece-g-full.md addressed (20 findings, zero deferrals). Substrate sprint 240 CLOSED — SessionStarted instrument wired.** Four-lens review closed in one pass per "nothing is deferred." Load-bearing find: SDD-1 + SUB-1 + SUB-3 (folded) — substrate declared `SessionStarted` as one of eight session Structs in 2026-06 (sprint 202); comment claimed sprint 209 would wire the RunStarted instrument; grep showed the class had zero emit sites for two months. Substrate sprint 240 landed: `_session_started_factory` closes over the ten SessionStarted fields; `b.instrument("session_started", on=api.RUN_STARTED, ...)` inside `session_topology`; two new session_topology kwargs (`workspace_shape`, `bundle`) thread the missing manifest fields; companion edit in `substrate-ui/server.py::_build_session_topology_from_manifest` passes them from the manifest. Three pytests PASS: SessionStarted fires exactly once; every schema field populated; precedes UserMessage on the record. **Substrate-ui SUB-1 fix (same commit):** `terminal.ts::_openSession` no longer emits `DRIVER_SESSION_STARTED` on POST-ack; `_handleEnvelope` gains a `SessionStarted` branch that fires the tag with the substrate-side fields (`driver_model`, `driver_context_tokens`, `bundle`, `parent_session_id`) once the record's SessionStarted envelope lands via SSE. One vocabulary per event; the record is the source of truth. **ARCH-2** landed: new `web/state.ts` exports `AppState` interface + `createAppState()` factory; app.ts replaces the literal STATE declaration with typed `const STATE: AppState = createAppState();` — tsc error count drops 666 → 595 (71 cascading errors gone). **ARCH-1** landed as a plan doc (`process/refactor-reviews/PLAN-2026-08-28-web-app-ts-split.md`), dispatch after sprint 038 to avoid merge conflicts with the piece-G queue. **CQ-1** two tsc errors on capture-grade.ts fixed via `FixtureKind` type; new `check:tsc-new` script wired into `npm run signals` (greps tsc output for any error in piece-G new files; fails the gate if any new file gains one). **CQ-2** typed sprint 033's `_snapshotView`/`_restoreView` helpers (`interface ViewSnapshot`, `interface FocusSnap`). **CQ-3** replaced terminal.ts's 60Hz `promptTick` rAF loop with stateful `_updatePrompt()` called on session-open + session-close. **CQ-4** new `_postJson<T>()` helper returns typed `{ok:true,data}|{ok:false,failure_class,detail}` result; three fetch call-sites in terminal.ts refactored; error lines now name the failure class (`session: open failed [http] HTTP 500: ...`). **ARCH-5** all five 036 cards amended to specify `web/controls/<name>.ts` per the `mountTerminal` shape. **SDD-4** new `## Post-close addenda` section added to `process/WORKING_AGREEMENT.md` formalizing the CLOSEOUT-ADDENDUM pattern rule 12 discipline established. **SDD-6** disproven: verified against server.py that every 036 control sits on live daemon surface (036a driver PATCH live per 215c; 036b bundle PATCH live per 032b; 036c workspace create-only per card; 036d tools PATCH live per 217e; 036e isolate create-only per card). Response doc: `process/REVIEW-2026-08-28-piece-g-full-RESPONSE.md`. Gates: 68/68 parity; `check:tsc-new` clean; full `npm run signals` PASS; substrate pytest 240 PASS.

- **2026-08-28 — Sprint 032b CLOSED (substrate-ui) + substrate 238 CLOSED. The "blocked" queue drained.** Two prereqs that I had queued as pending — my own cards — landed today per Architect directive "nothing is deferred; blocked-on-X where X is my own card is laziness." Substrate 238: `list_bundles(bundles_root: Path | None = None) -> list[Bundle]` added to `substrate/src/substrate/bundles.py`. Enumerates shipped defaults (session bundle + every `<app>.bundle/` under topologies/applications/) and user bundles (subdirectories of `_bundles_root(bundles_root)` with a bundle.toml); user shadows shipped when a name collides. Five tests in `substrate/tests/test_bundles_list.py` PASS. Substrate-ui 032b: mid-session bundle PATCH. `_session_patch` lifts `bundle` from `_NOT_YET` to `_PATCHABLE`; `SessionRegistry.set_bundle(session_id, bundle)` added following the `set_driver` pattern (per-session lock; `_replace(manifest, bundle=...)`; atomic manifest.json write; bundle name validated via `substrate.bundles.load_bundle` so an unknown name is a 400 at PATCH time not a next-turn crash). PATCH response body now carries `bundle` so the UI can read post-state without a follow-up GET. **NO TranscriptCompacted envelope emitted** — the card's original spec called for one, but nothing gets compacted at bundle-change (only the next turn's seed shape changes); emitting a false-signal envelope would be worse than the absence of one. Six tests in `substrate-ui/tests/test_server_session_patch_bundle.py` PASS. Live daemon verified: `PATCH {bundle:"pair_coding"}` → 200 with `bundle:"pair_coding"` in body; `PATCH {bundle:"does-not-exist"}` → 400 "unknown bundle". Full `npm run signals` chain PASS after the daemon restart. Clears sprint 034a (`GET /api/bundles` consumes `list_bundles`) and sprint 036b (mid-session bundle picker). Piece-G queue at close: every remaining sprint truly unblocked — no card waits on any other pending prerequisite.

- **2026-08-28 — Sprint 035 CLOSED. Terminal column + session-turn wiring live end-to-end.** New `web/terminal.ts` module (`mountTerminal(el, {driverDefault})`) mounts inside `#view-terminal` (from sprint 033). Wires to the piece-B endpoints: `POST /api/session` opens the session lazily on first turn, `POST /api/session/<id>/turn` sends the user text, `POST /api/session/<id>/end` closes on `/exit`, `GET /api/session/<id>/events` streams the record via `EventSource` (SSE). Four driver_session tags fire at real code paths — DRIVER_SESSION_STARTED on POST /api/session ACK (substrate does not emit a SessionStarted envelope; the daemon ACK is the observable event), USER_MESSAGE_INJECTED on Enter, PARK_LANDED on `Park` envelope from SSE, DRIVER_SESSION_ENDED on POST /end ACK with a double-fire guard when the SSE also delivers `SessionEnded` moments later. New grader invariant `checkDriverSessionBookends`: every DRIVER_SESSION_STARTED matches one DRIVER_SESSION_ENDED with the same session_id; every USER_MESSAGE_INJECTED matches one PARK_LANDED with the same {session_id, turn_index}. New grader kind `--kind session` with EXPECTED_ORDER `[SESSION_INIT, VIEW_SWITCHED, DRIVER_SESSION_STARTED, USER_MESSAGE_INJECTED, PARK_LANDED, DRIVER_SESSION_ENDED]`. **Side-fix (server.py):** `_record_path` extended to resolve session-id records under `_SESSIONS_BASE/<sid>/record` when name matches `^s_[0-9a-f]{8,32}$`, so the legacy dock's `followLive` polling can still reach sessions returned by `/api/agent` (piece B rewired /api/agent to session-shaped responses; the legacy shape was `{name}` and the console fixture broke on `waitFor AGENT_LAUNCHED` + `FINAL_ANSWER_RENDERED`). **Side-fix (web/app.ts sendChatMessage):** accepts both response shapes — legacy `res.name` OR piece-B `res.session_id` / `res.record.split(...)`. **Race-fix (harness):** view-toggle harness waits on `document.activeElement.id === "launchsel"` before asserting focus restore, because the state flip fires before the rAF-scheduled restore lands. New harness `harness/capture_terminal_session.js`: 15 assertions PASS end-to-end against a live deterministic session — session open, two turns (Enter + reply cycles), /exit end, all four bookend tags fire, session_id stable across start→end, PARK_LANDED matches USER_MESSAGE_INJECTED on {sid, turn_index}. Fixture written to `captures/sprint-035/terminal-session.jsonl` (16 signals) and graded by the new `checkDriverSessionBookends`. Two screenshots viewed: `screenshots/35-terminal-view-post-user-message.png` (session started + user's `> hello`), `screenshots/35-terminal-view-post-model-reply.png` (adds the deterministic model's stub reply + parked indicator). Package.json chains `capture:terminal-session` + `grade:terminal-session` into `npm run signals`. Gates: `check:vocab-parity` 68/68 (61 emit sites, all locked — up from 55 as terminal.ts adds four new emit sites); full `npm run signals` chain PASS across 4 consecutive runs. Cleared to dispatch: 037a (session harness reuses terminal.ts), 037b + 037c (removed dependency on legacy dock's chat now that session records resolve via /api/records).

- **2026-08-28 — Sprint 034a CLOSED. Server endpoints live: GET /api/records?exclude_sessions=true + GET /api/bundles.** `server.py::_records_index(exclude_sessions=False)` extended with the filter — drops names starting with `_SESSION_PREFIXES` (`launch_`/`build_`/`resume_`) or matching `_SESSION_ID_RE` (`^s_[0-9a-f]{8,32}$`). New `_bundles_index()` consumes `substrate.bundles.list_bundles()` (substrate sprint 238, closed today); response shape per bundle: `{name, description, tools_enabled, slot_count}` — slot_count is the count of the three prose slots (methodology/personality/per_turn) carrying text. `do_GET` gains two dispatch branches. New test file `test_server_records_bundles.py` — 6 cases PASS: default records list; exclude_sessions filter is subset; session-prefixed names filtered; bundles lists ≥2 shipped (session + apps); bundle shape shape check; bundles sorted. Live verification: `GET /api/bundles` returns 5 bundles (`best_of_n_verified`, `code_review`, `pair_coding`, `research_sweep`, `session`); `GET /api/records?exclude_sessions=true` returns 30 records with zero session-prefixed names. Full `npm run signals` chain PASS (no regression on existing `/api/records` consumers — the studio + console fixtures unchanged). Cleared: **sprint 034b** (four-bucket rail — consumes both endpoints) + **sprint 035w** (create-time controls dialog — consumes /api/bundles for the picker). Next by the project's logic: 035w (terminal-view create-time controls) — the remaining piece of the mechanical translation before 037c retires the dock.

- **2026-08-28 — Sprint 035v CLOSED. Params drawer + /set slash live in the terminal.** Consumes 032c (`SessionManifest.driver_params` + PATCH) and v0.7.2's `DRIVER_PARAMS_PATCHED`. Terminal header gains `<span id="terminal-params">` between driver picker and hint (`think X · tokens Y · timeout Zs`; ∞ when unset). `_formatParamsHint(params)` renders from a driver_params dict. `TerminalHandle` gains `driverParams` + `pendingDriverParams` + `updateParamsHint`. `mountTerminal` installs `_updateParamsHint`. `_openSession` threads `pendingDriverParams` into POST body + adopts the ACK's `driver_params` echo (per sprint 032c). `_closeStream` resets `driverParams` + refreshes hint. `_endSession` now synchronously calls `_closeStream` after emit — fixes the async race where the SSE-driven SessionEnded arrived AFTER the /exit test measured the hint. Slash `/set [key] [val]`: `/set` no-args prints current; `/set think on|off` toggles; `/set tokens N` (N ≥ 0; 0 = uncapped); `/set timeout N` (N > 0); `/set num_ctx N` (N ≥ 1); per-key validation with clear usage hints on bad input; unknown key rejected. With no session: queues on `pendingDriverParams`. With session: PATCH `/api/session/<id> {driver_params: <merged>}` + emit `DRIVER_PARAMS_PATCHED{session_id, params, prior_params}` + hint refresh. New harness `capture_terminal_params_drawer.js` — 16 assertions PASS end-to-end: mount, queue-before-session, /set no-args, session-open adopts queued, PATCH mid-session fires emit + refreshes hint, three bad-input rejections, reverse-direction PATCH, /exit closes + resets. Wired into `npm run signals` as `capture:terminal-params-drawer`. Screenshot `screenshots/035v-params-drawer.png` viewed: full header `driver [deterministic ▾] · think on · tokens 4096 · timeout 300s · type to talk · /exit to leave · /help`. Gates: check:vocab-parity 69/69 (65 emit sites, up from 64 — DRIVER_PARAMS_PATCHED live); check:tsc-new clean; full `npm run signals` chain PASS across eight fixtures. Cleared: 037c dock-retirement precondition #4. The daily-driver terminal now carries the full dock control set — driver picker + params row + slash router + interrupt + all product-spec §2a slashes + /set. Mechanical translation of the agent terminal into the daily-driver terminal is functionally complete for mid-session mutation. Next: 035w (create-time controls dialog, waits on 034a).

- **2026-08-28 — Sprint 035u CLOSED (batch 2, item 3 — batch 2 complete). Ctrl+C interrupt live in the terminal.** Per PRODUCT-SPEC §2: "Ctrl+C interrupts the current turn without ending the session." `web/terminal.ts` gains a keydown handler on `#terminal-input` that intercepts Ctrl/Cmd+C when the input has focus AND has no selection (selection-preserved copy stays as browser default). No-session → prints hint. Active-session → `POST /api/session/<id>/interrupt` (piece B 217d); response `{interrupted, landed, session_id}` drives one of three body lines: "^C interrupt (landed)", "^C interrupt (dispatched — envelope arriving on /events)", or "^C — no turn in flight; type /exit to end session". Complements sprint 035s's `/interrupt` slash — one behavior, two entry points, same wire. No new v0.7 tag (the interrupt's effect lands on the record as `substrate.ProducerCancelled`; already observable via SSE). New harness `capture_terminal_interrupt.js` — 7 assertions PASS: Ctrl+C with no session prints hint; Ctrl+C on idle-parked session prints "no turn in flight"; Ctrl+C with selection in input does NOT fire interrupt (interrupt-line count stable, copy path preserved); Ctrl+C with no selection AND active session fires interrupt path; /exit closes; no uncaught page errors. Wired into `npm run signals` as `capture:terminal-interrupt`. Gates: check:vocab-parity 69/69 (64 emit sites, unchanged); check:tsc-new clean; full signals chain PASS across seven fixtures. Cleared: 037c dock-retirement precondition #3. **Batch 2 complete — all three independent UI cards landed (035s slash router + 035t driver picker + 035u interrupt).** Batch 3 next: 035v (params drawer, waits on 032c which is closed — actually 035v can dispatch now) + 035w (create controls, waits on 034a which is pending). 037c dock retirement waits on 035v + 035w. 034b + 036a-f + 038 remain per the fold's parallel piece-G queue.

- **2026-08-28 — Sprint 035t CLOSED (batch 2, item 2). Driver picker live in the terminal header.** Complements 035s's `/model` slash — one control, two entry points, one wire (`PATCH /api/session/<id> {driver}`). `_mkChildren` returns a `driverSelect` alongside body/input/prompt; new `_populateDriverPicker(select, h)` reads `GET /api/models` on mount, dedupes across `models + cli + "deterministic"` (20 options at this box's tag list), sets initial value from `opts.driverDefault` when present in the option list; `mountTerminal` wires `driverSelect.addEventListener("change", ...)`. Change with active session: `_fetch PATCH driver` + emit `DRIVER_PATCHED{session_id, driver, prior_driver}` + update `h.driverName` + `_updatePrompt()` (prompt refreshes to `${driver} ›`). Change with no session: update `h.driverName` in-place + print "queued for next session" (the next `_openSession` uses it). Revert on PATCH failure. New harness `capture_terminal_driver_picker.js` — 12 assertions PASS end-to-end (picker mounts + populates, initial value correct, queue-when-no-session works and does not fire the tag, session-open + swap fires `DRIVER_PATCHED{driver=kimi-k2.6:cloud, prior_driver=deterministic}`, prompt refreshes, second swap fires reversed direction, `/exit` closes). Wired into `npm run signals` as `capture:terminal-driver-picker`. Screenshot `screenshots/035t-driver-picker.png` viewed: driver dropdown sits between title and hint, hint reads "type to talk · /exit to leave · /help." Gates: check:vocab-parity 69/69 (64 emit sites, unchanged — no new tag); check:tsc-new clean; full `npm run signals` chain PASS across six fixtures now. Cleared: 037c dock-retirement precondition #2. Next: 035u (Ctrl+C interrupt).

- **2026-08-28 — Sprint 035s CLOSED (batch 2, item 1). Slash router live in web/terminal.ts.** Ports substrate/src/substrate/cli.py::_slash_route (line 1053) into the browser terminal per PRODUCT-SPEC §2a's nine-slash contract + FEATURE-MAP's twelve-slash target. Twelve slashes wire to live daemon endpoints: `/model` → PATCH driver + `DRIVER_PATCHED`; `/tools` → PATCH tools + `TOOLS_RESTRICTED`; `/bundle` → PATCH bundle + `BUNDLE_ATTACHED`; `/context <lo-hi> [--kind K]` → stashes on handle, next `_sendTurn` threads into POST body's `context` field (piece B sprint 217e); `/inspect` + `/narrate` → GET /api/records/<name>/narrate; `/tail` → GET /events; `/cat <seq>` → GET /events + payload pretty-print; `/list [records|topologies|sessions|applications|bundles]` → the corresponding GET; `/run <app>` → POST /api/topology/<name>/run; `/diff` → GET /api/worktree_diff via session workspace; `/studio` → window.open('/studio.html', '_blank'); `/interrupt` → POST /api/session/<id>/interrupt; `/help` → prints the slash inventory. Three print hints: `/replay` (daemon endpoint not exposed; CLI-only), `/list bundles` (sprint 034a pending), unknown-slash error. Two typed HTTP helpers added alongside `_postJson`: `_fetch<T>(url, method, body)` for POST/PATCH; `_fetchGet<T>(url)` for GET. Both return the same discriminated `FetchResult<T>` union naming failure class (network/http/parse). `TerminalHandle` extended with `pendingContext` and `currentRecord`. Keydown handler routes every non-empty input through `_slashRoute`; falls through to `_sendTurn` if not a slash. New harness `capture_terminal_slash_router.js` — 20 assertions PASS end-to-end (all slashes + emits + hints + unknown-slash error + /exit). Wired into `npm run signals` as `capture:terminal-slash-router`. Rule-6 stretch acknowledged: web/terminal.ts grows from ~360 to ~600 lines, one concept (slash router mechanically ported), precedent piece D's own _slash_route. Gates: check:vocab-parity 69/69 (64 emit sites, all locked — up from 61 as three new PATCH-emit sites land); check:tsc-new clean; full `npm run signals` chain PASS across all five fixtures now (console + studio + view-toggle + terminal-session + slash-router). Cleared: 037c dock-retirement precondition #1 (slash router in place). Next: 035t (driver picker in terminal header).

- **2026-08-28 — Batch 1 of mechanical-translation fold CLOSED. Sprint 032c (SessionManifest.driver_params) + v0.7.2 vocab (DRIVER_PARAMS_PATCHED) live.** Piece-G FOLD-2026-08-28-piece-g-mechanical-translation batch 1 executes per Architect ratification. One tag one contract (grammar rule 1): `NEW_TAG_PROPOSED` for `DRIVER_PARAMS_PATCHED` — not folded into `DRIVER_PATCHED` to avoid the tag polymorphism v0.7.1 TAG_SPLIT retired for `PANE_SWITCHED`. Sprint 032c: `SessionManifest` gains `driver_params: dict[str, Any] | None`; `SessionRegistry.set_driver_params(sid, params)` new method with per-key validation (think:bool / max_tokens:int≥0 / timeout:float>0 / num_ctx:int≥1); `_RESPONDER_CACHE` retyped to `(name, params_tuple)` so per-session driver-param combos yield distinct Responder instances; `_daemon_driver_resolver(name, params)` threads params into `OllamaResponder(think=..., max_tokens=..., num_ctx=..., timeout=...)` for the Ollama branch (ignored for CLI + Deterministic branches); `_session_create` accepts + validates `driver_params` at create-time with rollback-on-bad-shape; `_session_patch` lifts `driver_params` into `_PATCHABLE`; POST + PATCH response bodies carry `driver_params`; `_build_session_topology_from_manifest` threads `manifest.driver_params` to the resolver. Nine pytests in `test_server_session_driver_params.py` PASS. Live daemon verified: `POST {driver_params:{think:true, max_tokens:4096}}` → 200 with echo; `PATCH {driver_params:{think:false, timeout:600.0}}` → 200; `PATCH {driver_params:{bogus:1}}` → 400 with allowed-keys list. **Substrate 235 canceled** — Responder is constructed pre-topology so no substrate-side change needed; full fix lives on substrate-ui side. Gates: `check:vocab-parity` 69/69 (61 emit sites, all locked); full `npm run signals` chain PASS across all four fixtures. Cleared to dispatch: sprint 035v (params drawer + /set slash in web/terminal.ts). Batch 2 next: 035s (slash router, 12 slashes at once, no substrate deps) + 035t (driver picker) + 035u (Ctrl+C interrupt), all independent. Also on disk this pass: `process/PLAN-2026-08-28-piece-g-mechanical-translation.md` (my earlier two-phase plan), `process/REVIEW-2026-08-28-daily-driver-terminal-vs-spec.md` (Architect-diagnosed confusion), `process/FEATURE-MAP-2026-08-28-agent-terminal-to-daily-driver.md` (nineteen features × nine sections), `process/FOLD-2026-08-28-piece-g-mechanical-translation.md` (consolidation of the three into one queue). Sprint 032c card at `sprints/sprint-032c-session-driver-params.md`.

- **2026-08-28 — REVIEW-2026-08-28-piece-g-work-so-far.md addressed (twenty findings, zero deferrals). v0.7.1 LOCKED. Sprints 033a + substrate 239 CLOSED.** Every H + AP finding closed in one pass per Architect directive "nothing is deferred." Vocab v0.7.1 lands TAG_SPLIT: PANE_SWITCHED reverts to four inner-pane values, new VIEW_SWITCHED{to_view, prior_view, subject_record} takes the two view-scope values (closes H2 + AP5). Code refactor: web/app.ts _toggleView emits VIEW_SWITCHED; toggle binds to mousedown+preventDefault (kills global focusin listener + _lastRealFocus module state; addresses AP3); _snapshotView walks [id] elements only (AP2); .focus() uses preventScroll: true (a REAL bug the H3-tightened harness caught on first run — .focus() triggered scrollIntoView on the ancestor col, silently drifting scroll from 240 to 405); catch narrowed to DOMException (AP7). New surfaces: web/view-ids.ts (VIEW_IDS const, AP4), web/observability.ts (installObservabilitySurface with typed interface, AP6). Grader: checkViewSwitched enforces closed-set + desktop-side pairing invariant (H4). Harness rewritten: waitForFunction throughout (AP1), pageerror trap (H5), strict scroll equality with test-setup guard (H3), scroll seeded to 240 on test-rail-col + verified round-trip. package.json chains capture:view-toggle into npm run signals (H1). H6: sprint-033-language-pass.md retro-authored as `status: reverted-2026-08-17` audit trail. H7 closed by executing substrate 239 (added process/signals/current.json → 0.3.json symlink + WORKING_AGREEMENT registry row) and substrate-ui 033a (syncer follows the pointer, mirror bumped to substrate-0.3.json, vocabulary.ts import path bumped). AP8: sprint-033-CLOSEOUT-ADDENDUM.md enumerates every added helper the original card underspecified. Gates: check:vocab-parity 68/68; npm run signals full chain PASS (14 harness assertions including strict scroll equality). Response doc: process/REVIEW-2026-08-28-piece-g-work-so-far-RESPONSE.md.

- **2026-08-28 — Sprint 033 CLOSED. Two-view scaffold + header toggle landed.** `web/index.html` now wraps the console (cursor + body + term-row + health) in `#view-desktop` and adds a placeholder `#view-terminal` container; sprint 035 fills the terminal view. Header gains `#view-toggle` (⇄ view). `web/app.ts` gains `STATE.view: "desktop" | "terminal"` (default desktop), `STATE.viewSnap`, `_snapshotView()` / `_restoreView()` (per-view scroll + focus + selection preserve), `_toggleView()` emitter, and a focusin listener that tracks the pre-click focus so the mouse path (button steals focus) restores correctly. Ctrl+` rebound: was `termSetOpen()`, now `_toggleView("ctrl_backtick")`. The docked terminal still opens via `#termOpen` for one release; sprint 037c retires it. `tools/capture-grade.ts` `VIEW_TO_PANE_ID` gains `terminal` + `desktop`; new `VIEW_SCOPE_TO_PANE` set exempts view-scope switches from the pane-render pairing check (the flip itself is the observation; no downstream paint is contracted). New harness `harness/capture_view_toggle.js` — twelve assertions, all green: initial state, click flip, Ctrl+` flip-back, PANE_SWITCHED shape and payloads on both directions, focus restore across a click-out-and-back cycle, PANE_SWITCHED total count. Two screenshots viewed and clean: `screenshots/33-desktop-view-console.png` (full console visible under `#view-desktop`) and `screenshots/33-terminal-view-empty.png` (header + placeholder message; header controls carry the live record's FINALISED badge, proving the app state persists across the flip). Gates: `check:vocab-parity` 67/67, `npm run e2e` PASS (existing console flow unchanged), harness PASS. Cleared to dispatch: 034a, 034b, 035, 036a-e.

- **2026-08-28 — Sprint 032a CLOSED. Signal vocabulary v0.7 LOCKED (67 tags).** Adds five session-control tags in the existing `driver_session` category (`DRIVER_PATCHED`, `BUNDLE_ATTACHED`, `WORKSPACE_SELECTED`, `TOOLS_RESTRICTED`, `ISOLATE_TOGGLED`) motivating piece-G sprints 036a–e; adds one optional payload field `RECORDS_LOADED.bucket` (values `sessions | recent | bundles | records`) motivating sprint 034b. Strict superset of v0.6 — every prior tag, invariant, entity_merge, runtime_operator, and payload_types entry carries over unchanged. No new invariants; the driver-session pairing sits at the substrate wire level already covered by v0.6's `checkSessionBookends` block. Files: `signals/versions/0.7.json` (67 tags, 13 categories), `signals/versions/0.7-rationale.md` (per-tag rationale + proposal audit trail), `signals/versions/current.json` symlink repointed 0.6.json → 0.7.json. Three gates green: `check:vocab-parity` (67/67, 54 emitted, all locked), `npm run e2e` (console pass), `npm run signals` (both fixtures graded). Cleared to dispatch: 034b, 036a–e. Executor: Claude Opus 4.7 on Architect directive "do it" following REVIEW-2026-08-28-piece-g-plan-RESPONSE.

- **2026-08-17 — Workspace-level product spec landed (governs substrate-ui's next sprints).** New workspace-root folder at `../../current-design-direction/` holds two governing docs: `DAILY-DRIVER-2026-08-17.md` (vision — what substrate becomes as a daily driver for software engineering) and `PRODUCT-SPEC-2026-08-17-round8.md` (spec — sixteen sections covering the session topology, the substrate toolkit exposed to the session model, standing sub-agents, context injection + the seven-category bundle concept + on-disk storage, applications, the record + sub-record nesting + workspace nesting vs Claude Code, `--shared-workspace` toggle, failure modes, what ships today, seven build pieces, the UI two-views-one-toggle shape, the Claude Code comparison grounded in `docs/landscape/`). Eight rounds on disk (round 1 → round 8); versioning rule stated in trailer — every revision writes a new round-N file, no in-place edits. Rounds 6–8 add compaction (§4a: rolling window + `inspect_record` on demand in v1; summary+tail in v1.5), context bundles (§7b: seven categories, `~/.substrate/bundles/<name>/` layout, `bundle.toml`), and the two-view UI (§13: terminal-only view + desktop-LLM view under one button, supersedes Sprint 034's three-tab shape). Substrate-ui-3pane fork stays as the code home for the two-view rearrangement work when a sprint dispatches it; the fork lives at `../../substrate-ui-3pane/` and is not under git per Architect's ruling — it's a working copy, not a repo. Signal instrumentation v0.5 is unchanged by any of this; a v0.6 bump lands with the session topology sprint (three session-boundary tags).

- **2026-08-17 — SDD instrumentation arc CLOSED (Sprints 018–032, three days, three waves, five review passes).** Started 2026-08-14 on the Architect directive "initiate the sddification of substrate-ui." Ends today with signal vocabulary v0.5 locked (54 tags, 12 categories, 13 invariants, 5 entity_merges with retroactive ratification recorded in-file). Both surfaces of substrate-ui carry emit call sites for every tag they own: web/app.ts fires 43 tags across the console (records, panes, stream, terminal, agent, topology, diff, incidents); web/studio.ts fires 11 tags across the studio (session lifecycle + 9 studio-specific). Union: 54, matches the lock. Emitter throws on unknown tag, missing required field, or foreign-key drift (namespace-split against substrate's runtime vocabulary mirror). Parity gate scans web/ + harness/ + tools/. Grader enforces every locked invariant against two JSONL fixtures (console + studio). `npm run signals` chains parity → capture both → grade both → exit 0. Five vocabulary bumps (v0.1 → v0.5) as merited review findings folded in; every finding across four dated reviews on record with an outcome. Wave-1 = 018 TS+Vite + 019 vocab lock + 020 emitter/gate/grader + 021–028 per-subsystem wiring + 029 wave close. Wave-2 = 030 substrate_kind FK enforcement + 031 optional e2e signal tail. Wave-3 = 032 studio surface instrumentation (closed a scope gap the arc plan had scoped only to app.ts). KIT_DIARY entry filed 2026-08-17 with four new lessons (H9 confirmed, H10 confirmed, H2 falsified, H8 recurrence). Sprint 033 (UI-NEXT language pass) reverted the same day it landed — the Agent read "continue" as authorization to jump work streams; the revert is on the next entry.

- **2026-08-17 — Sprint 033 (language pass) REVERTED.** Architect-directed rollback. Sprint 033 was UI-NEXT item 1, a different work stream from the SDD arc; the Agent picked it up under "continue" without an explicit go. Every edit reverted: the four assay-board "the baseline" strings back to "the bar" (both `web/app.ts` and `web/index.html` CSS); rail meta "N approaches" back to "N arms"; inspector placeholder + selectRecord reset back to "Select an event or a Producer to trace its provenance"; I/O + diff + play tooltips restored; health message "halted resumably" and the "Finished is not worked" second-sentence restored. Three e2e assertions restored in lockstep (e2e_assay "gap vs the bar"; e2e_console `/Select an event or a Producer/` at two sites). Sprint 033 card deleted (no audit trail needed for reverted work). Four gates green after revert: build, console e2e, assay e2e, signals.

- **~~2026-08-17 — Sprint 033 (language pass, UI-NEXT item 1) CLOSED.~~** REVERTED same day; see entry above. First non-SDD sprint in this session — advances the July direction doc's substrate-ui-NEXT list. Filter White/Orwell across user-facing strings in `web/index.html`, `web/app.ts`, and the studio's rendered prose. Nine prose edits: assay board "the bar" → "the baseline" (four sites in app.ts + one CSS `content` in index.html); rail meta "N arms" → "N approaches"; I/O tooltip "toggle Inputs & Outputs" → "show inputs and outputs"; diff tooltip drops the stale "D-8" citation; play tooltip removes the duplicated "replay"; inspector placeholder "Select an event or a Producer to trace its provenance" → "Click an event or a Producer to see how it was caused" (two sites); health message "halted resumably — awaiting …" → "paused — awaiting …"; health message drops the repeat "Finished is not worked." Three e2e substring assertions updated in lockstep (e2e_assay "gap vs the baseline"; e2e_console `/Click an event or a Producer/` at two call sites). Direction-doc rules ("approach" not "arm", "baseline" not "the bar") now honored across every rendered surface. No locked tag or category renamed. Four gates green after: parity (54/54, unchanged), build, all three e2e (console + studio + assay), signals (both fixtures). Card at `sprints/sprint-033-language-pass.md`.

- **2026-08-17 — Sprint on M4 (inspectProducer race) CLOSED. Signal vocabulary v0.5 LOCKED.** Adds one tag — `PRODUCER_INSPECTION_RACED` (category stream, stratum incident, payload `instance` + `subject_record`) — closing REVIEW-2026-08-16 M4 finding at the Architect's explicit "just do the new tag" directive. Wired at `web/app.ts § inspectProducer`: when the lane click races the graph mutation, the emit fires instead of dropping to silence; the inspector still opens the /explain/<instance> chain in both branches. Rationale at `signals/versions/0.5-rationale.md`. Four gates green: parity 54/54, build, e2e, both fixtures.

- **2026-08-17 — REVIEW-2026-08-16-sdd-instrumentation-comprehensive.md addressed; signal vocabulary v0.4 LOCKED.** Ratified on Architect directive "apply any necessary fixes" (2026-08-17). Fifteen findings dispositioned:
  - **S1 (entity_merges skipped ENTITY_MERGE_PROPOSED) — FIXED.** v0.4 adds `entity_merges_ratification` block naming the two Architect directives that ratified the five merges post-hoc; the file itself carries the discipline note for future merges (proposal-then-ratification, not silent-then-record).
  - **S2 (2026-08-15 halt bypassed) — FIXED.** Four halt-proposals dispositioned on the v0.4 record: `TURN_SUBMITTED.turn_index` promoted from optional to required (evidence: `web/app.ts:342` always sets it); INSPECTOR_RENDERED + FRAME_RENDERED deferred with named reasons; TOPOLOGY_LAUNCHED split resolved without a split (Sprint 032's SPEC_BUILT covers the authored-build path).
  - **D1 (WORKING_AGREEMENT.md three vocabularies) — FIXED.** Line 25 + trailer rewritten to v0.3/current.json posture; version literal removed from those two paragraphs.
  - **D2 (v0.3 literal hardcoded across three files) — FIXED.** New `signals/versions/current.json` symlink → v0.4. `vocabulary.ts` and `check-vocabulary-parity.ts` both read from the symlink; v0.5 bump = one `ln -sf` + one new versioned file.
  - **D3 (rationale delta ledger, no BOOTSTRAP audit) — FIXED.** `signals/versions/0.4-rationale.md` carries the full BOOTSTRAP shape: intent+scope, per-layer decisions (Layers 0–7), a dual-contract audit table for the nine studio tags, project-specific overrides, open proposals for v0.5.
  - **M1 (chat-turn-count accepts both shapes) — FIXED.** Grader now enforces `declared === turns*2` (normal) or `declared === turns*2 - 1` (in-flight assistant reply), not "either shape."
  - **M2 (grader frame-monotonic beyond lock) — FIXED.** Invariant added to v0.4.
  - **M3 (SESSION_INIT-first + SESSION_ENDED-last not enforced) — FIXED.** Two invariants added to v0.4; new `checkSessionBookends` in grader enforces absolute position against the whole capture.
  - **M7 (parity gate skips tools/) — FIXED.** SCAN_DIRS now `["web", "harness", "tools"]`.
  - **M8 (parity gate docstring stale) — FIXED.** Docstring updated to name Sprints 019 + 020 + 021–028 as the instrumentation lineage.
  - **M10 (SDD-ARC-PLAN.md missing -superseded rename) — FIXED.** New `process/SDD-ARC-PLAN-2026-08-17-superseded.md` names the current status and points readers at ROADMAP-2026-08-16 + the sprint cards + `signals/versions/current.json`.
  - **M4 (inspectProducer race drops evidence) — DISCARDED with reason.** Adding a `PRODUCER_INSPECTION_RACED` tag would require another halt-and-articulate cycle for what is a one-in-a-hundred timing corner. The drop-instead-of-lie behavior is the correct trade for the current shape.
  - **M5 (Sprint 031 fixtures un-graded) — DEFERRED with reason.** The tail is explicitly a diagnostic seam, not a contract; grading would recouple DOM to signal contracts (the coupling Sprint 029 declined). Retention policy: the fixtures overwrite on next flagged run; nothing prunes them because they are gitignored (`.gitignore` covers `captures/e2e-*.jsonl`).
  - **M6 (studio harness under preventDefault, not target="_blank") — DEFERRED with reason.** The emit-firing behavior my code owns is proven; the browser's new-tab decision under target="_blank" is not my code's contract. A Playwright new-page test would double-cover the same emit for a small perceived gain.
  - **M9 (console↔studio transition off-record) — DEFERRED with reason.** The two-tab flow between the pages is a browser session shape; substrate-ui's per-page signals buffer captures each surface honestly. A cross-page fixture would require a shared session persistence layer that does not exist today.
  - Full ledger at `signals/versions/0.4-rationale.md § Per-change ledger` and `§ Halt-proposal dispositions`.

- **2026-08-16 — Sprint 032 (studio surface instrumentation) CLOSED. Signal vocabulary v0.3 LOCKED.** Closes the scope gap the Architect surfaced 2026-08-16 ("Studio must be instrumented; it is one thing, not two"). Bumps v0.2 → v0.3: adds category `studio`; nine new tags (SPEC_ROW_ADDED, SPEC_ROW_REMOVED, CANVAS_TOGGLED, SPEC_VALIDATE_REQUESTED, SPEC_VALIDATED, SPEC_BUILD_REQUESTED, SPEC_BUILT, SPEC_BUILD_REJECTED, CONSOLE_LINK_FOLLOWED); two new invariants (validate + build pairings); one new entity_merge (Spec ↔ authored_topology). Total tag count 53. Wired every action in `web/studio.ts` — button clicks, row removal, view toggle, validate/build brackets, console-link click. Studio anchor now uses target="_blank" so real users open the console in a new tab without nuking the studio's signals buffer. New harness `harness/capture_studio_signals.js` drives /studio.html through one authoring round-trip; new `npm run capture:studio-signals` + `npm run grade:studio-signals` scripts; `npm run signals` now chains both console and studio fixtures. Grader gained `checkStudioPairings` (SPEC_VALIDATE_REQUESTED → SPEC_VALIDATED within 5 s; SPEC_BUILD_REQUESTED → SPEC_BUILT|SPEC_BUILD_REJECTED within 30 s) behind a `--kind studio` flag. Both fixtures grade green: console 371 signals / 37 in-order / all pairings pass; studio 11 signals / 10 in-order / both new pairings pass. Two near-miss faults caught at close: replace_all on version strings turned `substrate-0.2.json` into `substrate-0.3.json` (fixed to keep the mirror filename), and the .consolelink click ate the studio's buffer via anchor navigation (fixed with a capture-phase preventDefault in the harness). Rationale at `signals/versions/0.3-rationale.md`. Card at `sprints/sprint-032-studio-instrumentation.md`.

- **2026-08-16 — Sprint 031 (optional signal-capture tail on e2e harnesses) CLOSED.** Sprint 029 declined coupling DOM contracts to signal contracts; Sprint 031 lands the reversible middle. New `harness/lib/capture-tail.js` exports `maybeCaptureTail(page, name)` — with `CAPTURE_SIGNALS` unset it returns immediately; with `CAPTURE_SIGNALS=1` it writes `captures/e2e-<name>.jsonl`. Wired into all four standing e2e harnesses (e2e_console, e2e_studio, e2e_assay, e2e_delegate) — one require + one call each. Verified: flag OFF writes no fixture, e2e passes cleanly; flag ON writes four fixtures (console 571 signals, studio 0 signals — honest, studio.ts has no emit call sites today, assay 11, delegate 28), e2e still passes. Failure in the tail logs and moves on — the DOM pass/fail is orthogonal to the capture. Main `npm run signals` gate still green. `captures/README.md` documents the flag + fixture paths. Card at `sprints/sprint-031-optional-signal-tail-on-e2e.md`.

- **2026-08-16 — Sprint 030 (substrate_kind foreign-key enforcement) CLOSED.** Promotes `payload_types: { kind: substrate_kind }` on EVENT_INSPECTED and PRODUCER_INSPECTED from documentary to runtime-enforced. Closes the mapping-review F3 finding at its enforceable end. Mirrors substrate's 66 KB `signals/0.2.json` into `signals/mirror/substrate-0.2.json` (sha256 9c1c97f7852ab8e01f54208273be64fa84861b4ebe458371ca28e5bacb726168), refreshable via new `npm run sync:substrate-vocab`. Vocabulary loader now exposes SUBSTRATE_KINDS (12 runtime tags) and SUBSTRATE_PRODUCER_KINDS (empty in current substrate ontology; falls through the empty-domain branch). Emitter enforcement is namespace-split: values in the `substrate.*` namespace must be in the closed set (catches typos like `substrate.RunStartedd`); other-namespace values need only be non-empty strings (application kinds like CodeChunk, ToolCall, Critique that substrate's vocab doesn't enumerate). `inspectProducer`'s "unknown" fallback drops instead of emits. Bundle grew 55 KB → 87 KB (the mirrored substrate vocab bundles in). Four gates: parity green (44/44 emits), build green, parent e2e green (after the initial closed-set-too-tight fault surfaced and got fixed to the namespace-split shape), signals grader green (325 signals, 37/37 in-order, all pairings pass). Card at `sprints/sprint-030-substrate-kind-enforcement.md`.

- **2026-08-16 — Roadmap recentered.** Written `process/ROADMAP-2026-08-16.md`. Supersedes `process/ROADMAP.md` (2026-06-23) — that doc predates the terminal-v1 pivot, the July cockpit-direction rulings, and the August SDD-instrumentation Wave-1. New roadmap: signal vocabulary v0.2 locked; SDD-instrumentation Wave-1 closed; substrate-ui-NEXT (skin, movable panes, real PTY, standalone wrapper, language pass, one-session-one-record, transcript view, MCP, provider parity, assay carry, thinking capture) held behind substrate-side workflow parity per `docs/cockpit/SUBSTRATE-UI-NEXT-2026-07-31.md`; three Wave-2 SDD candidates in this repo don't need the hold lifted. Prior 2026-06-17/23 Architect rulings triaged (static topology view RESOLVED at Sprint 002; substrate repo signposting is not this project; CliResponder folds into UI-NEXT item 9).

- **2026-08-16 — Signal vocabulary v0.2 LOCKED; all merited review findings across four dated reviews folded.** Ratified on Architect directive "fix all review findings that have merit" (2026-08-16). New file `signals/versions/0.2.json` (44 tags, 11 categories with `pane` replacing `view`); new rationale `signals/versions/0.2-rationale.md` with per-change ledger; v0.1 stays on disk as audit trail. Six merited fixes: category `view` → `pane` (frees the word for substrate's runtime primitive, F2 mapping); tag `VIEW_SWITCHED` → `PANE_SWITCHED` with payload `to_pane`/`prior_pane` (F2 mapping); structural block `view_payload_universal` → `pane_payload_universal` (follows category rename); `payload_types: { kind: substrate_kind }` on EVENT_INSPECTED + PRODUCER_INSPECTED (F3 mapping, documentary in v0.2); top-level `entity_merges[]` naming Record↔Run, Topology↔Topology, AgentRun↔Run, Event↔Envelope (F1 mapping); top-level `external_vocabulary_ref` pointing at substrate's `signals/0.2.json` (F4 mapping, matched by a canonical-home-registry row landed same session); top-level `runtime_operators[]` declaring Layer 6 with SubstrateReader, SubstrateController, ReplayEngine, AgentTurnAccumulator (F14 port-plan, F7 mapping); notes[0] reworded to name the reader+controller shape honestly (F5, F6 mapping). Vocabulary loader (`web/instrumentation/vocabulary.ts`), parity gate (`tools/check-vocabulary-parity.ts`), and all emit call sites in `web/app.ts` re-pointed at 0.2. Three findings discarded with reason (port-plan F4, F11, F13 — see 0.2-rationale.md § What is discarded). `npm run signals` green end-to-end: parity 44/44 distinct emits all locked; grader 37/37 in-order, all pairings pass, 208 paints monotonic, incident payloads clean. Fixture at `captures/sprint-021/console.jsonl`, 317 signals.

- **2026-08-15 — Wave-1 CLOSED (Sprint 029: harness integration + Wave close).** substrate-ui carries a locked signal vocabulary at v0.1 (44 tags, 11 categories, 8 invariants); every tag fires at a real emit site in `web/app.ts` (parity gate: 44/44 distinct emits locked); every invariant grades from one JSONL fixture (`captures/sprint-021/console.jsonl`, 333 signals) through 8 invariant checks (contains-in-order, RECORD_SELECTED→RECORD_LOADED exactly-one/staleness-drop, VIEW_SWITCHED→pane-render, frame monotonic, inspector payloads, TURN_SUBMITTED chat-window, agent launch+terminate exactly-one, topology launch+reject exactly-one, incident payload-content, chat turn count, DIFF pairing). `npm run signals` reproduces the four-gate close end-to-end (parity → build → capture → grade). The arc plan's "extend each e2e_*.js with three tail steps" was deliberately declined — coupling DOM contracts to signal contracts on every e2e edit would create noise; the single `harness/capture_signals.js` is the right shape and lives beside the standing e2e suite. Sprint 022 and Sprint 028 each caught a real grader bug at close (window sizing) — the discipline surfaced its own weaknesses, both fixed to the same "bounded at next request" pattern. Card at `sprints/sprint-029-harness-integration-wave-close.md`; `captures/README.md` documents the fixture policy.

- **2026-08-15 — Sprint 028 (incident coverage) CLOSED. Vocabulary v0.1 now fully wired — 44/44 tags.** Three emit sites, all on error paths: `FETCH_FAILED` at the single `api(p)` seam (non-2xx OR network throw both emit `{endpoint, status_or_error}`); `LAUNCH_REJECTED{kind, reason}` on the three launch/resume/agent branches with `kind ∈ {agent, topology, resume}`; `POLL_TIMEOUT{run_name, elapsed_ms}` in `followLive()` when the elapsed poll time exceeds the timeout param (clamped [30 s, 10 min]). Grader added `checkIncidentPayloads` — payload-content constraint per vocab § invariant #11 (Layer 7: content, not counter). Harness triggers LAUNCH_REJECTED by injecting a bogus option into `#launchsel` and clicking launch; triggers FETCH_FAILED by calling `window.api("/api/records/DOES_NOT_EXIST")` through the exposed seam. `checkTopologyLaunch` bug caught + fixed: earlier LAUNCH_REJECTED double-counted against a prior success within its 5 s window; corrected by bounding the window at the NEXT TOPOLOGY_LAUNCH_REQUESTED (same staleness-drop pattern as the RECORD_SELECTED check). Four gates: parity green (44 distinct emits — full lock coverage), build green, parent e2e green, grader green (37/37 in-order, all pairings pass, incident payloads FETCH_FAILED=1 LAUNCH_REJECTED=1 POLL_TIMEOUT=0 all valid). Zero halted, zero surfaced. Card at `sprints/sprint-028-incident-coverage.md`.

- **2026-08-15 — Sprint 027 (diff + assay) CLOSED.** Five emit sites: `DIFF_REQUESTED` at `#diffsel.onchange`; `DIFF_RENDERED` at both branches of `renderDiff()` (equivalent → first_divergence_seq=-1 sentinel; divergent → real seq); `ASSAYS_LOADED` at `loadAssays()` tail (count=0 in this env is legitimate); `ASSAY_SELECTED` at `selectAssay()` entry; `ASSAY_REPORT_LOADED` after the fetch resolves and passes the staleness guard (carries arm_count, case_count, optional verdict). Grader added DIFF and ASSAY pairings (both 5 s window, matching-key checks); DIFF_RENDERED added to the frame-monotonic pane-render set. Harness picks the second diff option early after boot, then conditionally clicks the first `.assay` row when present. Four gates: parity green (41 distinct emits, all locked), build green, parent e2e green, grader green (35/35 in-order, 1 diff pair; assay pair vacuous in this deployment). Rubber Duck caught + fixed an EXPECTED_ORDER placement bug — DIFF_REQUESTED was listed after STUDIO_OPENED while the harness fires it near the top; moved to its actual slot between HEALTH_RENDERED and VIEW_SWITCHED. Zero halted, zero surfaced. Card at `sprints/sprint-027-diff-assay.md`.

- **2026-08-15 — Sprint 026 (topology + launch) CLOSED.** Five emit sites: `TOPOLOGY_LAUNCH_REQUESTED` + `TOPOLOGY_LAUNCHED` at `#launchbtn.onclick`, `RESUME_REQUESTED` + `RESUMED` at `#resumebtn.onclick`, `STUDIO_OPENED` from a new header `#studiolink` (target="_blank"; emit fires synchronously on click, current page + buffer survive). Grader added `checkTopologyLaunch` (invariant #10: request → exactly one LAUNCHED or LAUNCH_REJECTED{topology} within 5 s, matching topology_name). Harness picks the first non-empty bundled topology, clicks launch, waits for RECORD_LOADED matching the new run_name (this fix caught by the initial rubber-duck run — the harness raced past `selectRecord`'s fetch chain to prune before the pair completed; grader flagged the unpaired RECORD_SELECTED, harness now waits), then clicks the studio link. RESUME_REQUESTED/RESUMED not exercised in this fixture (no paused resumable record in the session); the vocab contract doesn't require them per capture. Four gates: parity green (36 distinct emits, all locked), build green, parent e2e green, grader green (32/32 in-order, 4 record pairs, 6 view pairs, 1 topology-launch pair, 231 paints monotonic). Zero halted, zero surfaced. Card at `sprints/sprint-026-topology-launch.md`.

- **2026-08-15 — Sprint 025 (agent subsystem) CLOSED.** Five emit sites: `TURN_SUBMITTED` at `sendChatMessage()` entry (turn_index derived from convo.length/2); `AGENT_LAUNCH_REQUESTED` immediately before `POST /api/agent` with full params; `AGENT_LAUNCHED` on the POST's resolved run_name (optionally carrying `branch` in worktree mode); `AGENT_TURN_STREAMED` in `streamAgentTurns()` on every cycle with fresh events (carries new_events count + up_to_seq); `FINAL_ANSWER_RENDERED` when the same cycle observes a FinalAnswer (carries answer_length). Grader added `checkAgentLaunchTerminate` (invariant #5: request→launched|rejected within 1 s; invariant #6: launched→exactly-one FINAL_ANSWER_RENDERED|POLL_TIMEOUT with matching run_name; the old generic-Pairing entries for these were removed to prevent double-grading with weaker semantics) and `checkChatTurnCount` (CHAT_EXITED.turns_in_conversation must match TURN_SUBMITTED count in the window, accepting either convo-length or user-turn semantics). Harness forces the deterministic driver, sends one message inside the chat window, waits for FINAL_ANSWER_RENDERED before /exit. Four gates: parity green (31 distinct emits, all locked), build green, parent e2e green, grader green (29/29 in-order, all pairings pass, launch/terminate 1/1, chat turn count PASS, frame monotonic 237 paints). Rubber Duck: zero halted, zero surfaced. Card at `sprints/sprint-025-agent-subsystem.md`.

- **2026-08-15 — Sprint 024 (terminal subsystem) CLOSED.** Six emit sites: `TERMINAL_OPENED`/`TERMINAL_CLOSED` from `termSetOpen(v, trigger)` — `trigger` optional, tagged toggle_button on the two click paths and ctrl_backtick on the shortcut; `CHAT_ENTERED` in the `chat`/`agent` command branch with `reconnect` set when the convo has prior turns; `CHAT_EXITED` in the `exit` branch carrying `turns_in_conversation`; `MODEL_SELECTED` routed through a shared `_selectModel(next)` helper so both the picker onchange and the `model` command emit once with `prior_model` correctly threaded; `PARAMS_CHANGED` routed through `_setParam(field, value)` for think/tokens/timeout. Grader added `checkTurnInsideChatWindow` (vocab invariant #7: TURN_SUBMITTED only inside CHAT_ENTERED→CHAT_EXITED window; vacuous pass until Sprint 025 lands the emit) and 6 new tags in EXPECTED_ORDER. Harness extended: TERMINAL_OPENED → MODEL_SELECTED → three PARAMS_CHANGED → CHAT_ENTERED → CHAT_EXITED → TERMINAL_CLOSED before the prune, using `p.keyboard.press("Enter")` to submit each terminal command. Four gates: parity green (26 distinct emits, all locked), build green, parent e2e green, grader green (24/24 in-order, all pairings pass, TURN_SUBMITTED window vacuous pass). Rubber Duck noted a stronger check available in Sprint 025 — CHAT_EXITED.turns_in_conversation could be graded against TURN_SUBMITTED count inside the same window. Zero halted, zero surfaced. Card at `sprints/sprint-024-terminal-subsystem.md`.

- **2026-08-15 — Sprint 023 (stream + inspector) CLOSED.** Three emit sites: `STREAM_RENDERED` at `renderStream()` tail (carries `line_count`); `EVENT_INSPECTED` at `inspectEvent()` entry (`seq`, `kind`, `subject_record`); `PRODUCER_INSPECTED` at `inspectProducer()` entry, resolving `kind` from `STATE.graph.instances` with a "unknown" fallback for lane clicks that race the graph mutation. Grader added `STREAM_RENDERED` to the frame-monotonic pane-render set and a `checkInspectorPayloads` payload-content pass (non-empty required fields). Harness returns to run view before clicking the first `.ev` and the first `.lane`. Four gates: parity green (20 distinct emits, all locked), build green (53 KB main), parent e2e green, grader green (contains-in-order 18/18, VIEW→pane 6/6, 252 pane-paints monotonic, inspector payloads pass). Rubber Duck noted the `kind: "unknown"` fallback as a Drift-watchlist item — it would pass the current payload check but doesn't reflect a real producer kind; upgrading would require the manifest inside the grader. Zero halted, zero surfaced. Card at `sprints/sprint-023-stream-inspector.md`.

- **2026-08-15 — Sprint 022 (view subsystem) CLOSED.** Ten emit sites wired in `web/app.ts`: `VIEW_SWITCHED` from a shared `_switchView(to_view)` helper that only fires on an actual slot change and is called from gvRun/gvTopo/gvScene/modeToggle; `CURSOR_MOVED` from the seq oninput handler with `source` tagged by caller (drag | button | play_frame); `PLAY_STARTED`, `PLAY_STOPPED` (reason enum: user_pause | end_reached | scrub_interrupt), `SPEED_CHANGED` from the transport handlers; and the five pane-render tags (`GRAPH_RENDERED`, `TOPOLOGY_RENDERED`, `SCENE_RENDERED`, `IO_RENDERED`, `HEALTH_RENDERED`) at the tail of the matching `render*()` functions. Added a monotonic `_paintFrame` counter feeding `view_payload_universal = {frame, visible, pane_id, subject_record}` on every pane-render emit. Grader extended: `checkViewSwitchedRender` enforces the vocab invariant (the FIRST pane-render with matching subject_record after each VIEW_SWITCHED must be the expected pane, within 500 ms); `checkFrameMonotonic` enforces `frame` never decreases across pane-render tags. Harness extended to click gvTopo → gvRun → gvScene → modeToggle-to-io → back, change speed to 120, tap play/pause, drag the seq slider; fixture at `captures/sprint-021/console.jsonl` now 237 signals. Four gates: parity green (17 distinct emits, all locked), build green (53 KB main bundle), parent e2e green, grader green (contains-in-order 15/15, RECORD pair 2/2, VIEW→pane pair 5/5, frame monotonic 150 paints/150 peak). Rubber Duck caught + fixed two near-miss bugs: modeToggle was emitting `to_view: "run"` when the underlying graphView was scene (grader's VIEW_SWITCHED → GRAPH_RENDERED assertion caught the lie); the grader's initial "exactly one within 1000 ms" mis-modeled the invariant (play-frame paints at 120 fps piled up 71 SCENE_RENDERED matches) — corrected to "the next pane-render matches" with a 500 ms paint-cycle bound. Card at `sprints/sprint-022-view-subsystem.md`.

- **2026-08-15 — Sprint 021 (session close + records + record subsystem) CLOSED.** Five emit sites wired in `web/app.ts`: `SESSION_ENDED` on `beforeunload`; `RECORD_SELECTED` + `RECORD_LOAD_BEGIN` at `selectRecord()` entry (covers rail click, deep-link, and delegate paths in one site); `RECORD_LOADED` after the four projections land and the staleness guard passes; `RECORDS_PRUNED` after the your-runs clear affordance fires. Grader extended: the `RECORD_SELECTED → RECORD_LOADED` pairing rewritten to enforce the vocab invariant verbatim — exactly one matching-name `RECORD_LOADED` per `RECORD_SELECTED`, staleness-drop honored (a superseding `RECORD_SELECTED` before the load returns silences the first with no post-supersede `RECORD_LOADED` for the stale name). New harness `harness/capture_signals.js` drives one round-trip (auto-select → launch a fresh run → click a different record → prune) and dumps `window.__signals` to JSONL; first committed fixture at `captures/sprint-021/console.jsonl` (11 signals, both `RECORD_SELECTED → RECORD_LOADED` pairs match). Four gates: parity green (7 distinct emits, all locked), `npm run build` green, `npm run e2e` green (parent regression clean), `npx tsx tools/capture-grade.ts captures/sprint-021/console.jsonl` green (contains-in-order 7/7, pairing 2/2). Rubber Duck: zero halted, zero surfaced. Card at `sprints/sprint-021-records-record-session-close.md`.

- **2026-08-15 — REVIEW-2026-08-14-sdd-arc-plan.md addressed (12 findings).** Cards for sprints 018/019/020 renumbered from a colliding 008/009/010, moved to `substrate-ui/sprints/`, and backfilled honestly. `SDD-ARC-PLAN.md` revised to v2: sprint chain renumbered to 021–029, SESSION_ENDED wired into Sprint 021 (was unowned), Sprint 022 count corrected 9→10, graders for sprints 021/025/028 rewritten to match the vocab invariants verbatim (exactly-one/matching-name/staleness-drop; exactly-one termination; payload-content constraint not counter), self-referential cadence gate dropped, fixture policy cross-referenced to `SDD-HARNESS-PORT-PLAN.md`. Companion edits landed: `WORKING_AGREEMENT.md` clause that denied signals/ existence rewritten to state the current vocab-emitting posture; `tsconfig.json` `include` drops `harness` (Node scripts, not TS build). Review file kept on disk per hard rule 12; the arc plan carries a Revision log noting the twelve fixes inline.

- **2026-08-14 — Sprint 020 (SDD scaffold) CLOSED.** Ported Katybird's SDD instrumentation surface to substrate-ui: `web/instrumentation/sdd.ts` (typed `emit(name, payload)`, throws on drift, buffer exposed at `window.__signals`); `web/instrumentation/vocabulary.ts` (loads the locked JSON); `tools/check-vocabulary-parity.ts` (greps `web/` + `harness/`, cross-checks against lock, exit 0/1/2); `tools/capture-grade.ts` (JSONL reader + contains-in-order + pairing invariants + exit 0/1/2). Wired the first two emits at `web/app.ts`: `SESSION_INIT` at module load, `RECORDS_LOADED` after `/api/records` returns with count/run_count/demo_count payload. Dual contract: parity gate OK (44 tags, 2 emits, all locked); `npm run build` green (51 KB main bundle); `npm run e2e` (parent) still green end-to-end. Rubber Duck: one resolved-here — first draft redeclared `const runs`/`const demos` already used later in `loadRecords()`; esbuild caught it; fixed by inlining the counts. Card at `sprints/sprint-020-sdd-scaffold.md`.

- **2026-08-14 — Sprint 019 (vocabulary lock v0.1) CLOSED.** `signals/versions/0.1.json` locked at 44 tags across 11 categories (session, records, record, assay, view, stream, terminal, agent, topology, diff, incident) with 8 invariants + `view_payload_universal = {frame, visible, pane_id, subject_record}`. Rationale at `signals/versions/0.1-rationale.md` — per-layer decisions, dual-contract audit, four open v0.2 proposals. Agent-drafted at Architect direction (not the collaborative BOOTSTRAP.md § 1–11 walk — explicitly delegated); ratified by Architect on read-through. Parity gate caught a tag_count mismatch (declared 42, actual 44) on first run; corrected in place same session. Card at `sprints/sprint-019-vocabulary-lock.md`.

- **2026-08-14 — Sprint 018 (TypeScript + Vite conversion) CLOSED.** Behavior-preserving refactor. `web/app.js` → `web/app.ts`, `web/studio.js` → `web/studio.ts`; Vite build pipeline in place (`vite.config.ts`, `tsconfig.json` strict); `web/dist/` is the production output; `server.py` prefers `dist/` and falls back to `web/` if unbuilt; `<script type="module">` in both HTMLs; harness-compat shim at the tail of `app.ts` rebinds `STATE` + `loadRecords` + `selectRecord` + `loadAssays` + `loadModels` on `window` so the vanilla-JS Playwright harness scripts keep working unchanged. Dual contract: `npm install` clean, `npm run build` green (5 modules, 40+11 KB bundles), `npm run e2e` (parent) green end-to-end. Card at `sprints/sprint-018-typescript-conversion.md`.

- **2026-08-04 — W2.2 FOLLOW-ON: a live cockpit delegation is navigable (the display-only limit closed).** Sprint 017 proved the delegated-child branch on a committed demo but left real cockpit children display-only (they landed in the session workspace, unserved). Closed now: substrate `make_delegate` gained a `child_record_root(n) -> Path` hook (redirects the child RECORD; the workspace stays under the delegation dir); the cockpit `_agent` composes `delegate` into the real-model suites (ollama/cli) with `child_record_root=lambda n: RUNS/f"{base}_c{n}.record"`, so child records land as flat SERVED `runs/` records. **Live-verified (kimi-k2.6:cloud):** a real cockpit agent delegated "compute (2+3)*4"; the parent's ToolResult `child_root` = `runs/delegate_child_3e366fe6_c0.record` (served), `/api/resolve_child` returned its name, the child record is served + in the rail index (2 ToolCalls add/mul + FinalAnswer). So the delegated-child branch is navigable for a REAL run, not just the demo. **W2 phase gate MET:** an agent handed a splittable task delegates, the child is recorded + served + citable, and the flow shows the navigable branch. **Dual contract:** substrate 10 delegate tests (incl. `test_child_record_root_redirects_...`) + full suite; 30 server tests pass (server `_agent` refactored to extract the responder + compose delegate); all four e2e green. delegate substrate-side change committed in the substrate repo; the cockpit wiring here.

- **2026-08-03 — Sprint 017 (W2.2): the delegate child as a navigable branch in the parent's flow — CLOSED.** delegate (substrate W2.1) records the child as its OWN run and folds `{answer, child_root, steps}` into the parent's `ToolResult`; the UI showed that as one opaque line. Now the I/O surface renders it as a navigable branch. **Pressure-test first (in the card):** not already solved (no in-record linking / inner-root handling existed), and the real gap — a cockpit `/api/agent` run records to served `runs/` but its tool workspace is `~/.substrate/sessions/`, so real delegate children land UNSERVED. **The stitch, keeping the read surface `runs/`-only:** `server.py` `_resolve_child_name(child_root)` MATCHES the ToolResult's `child_root` path against the served set (never READS the arbitrary path) and `/api/resolve_child` returns the served name or null; `web/app.js` `renderIO` renders `↳ delegated child: <name>` (a cyan link → `selectRecord`) when servable, else display-only `↳ … recorded at <path>` (never a dead link), plus a `◂ parent` breadcrumb that returns; `gen_demo_records.py` generates a deterministic `demo_delegate` parent (a scripted `delegate` tool carrying the child's absolute record path) + `demo_delegate_child` (a real calculator run), both under `runs/`. **Observation contract, both tracks:** structural — `harness/e2e_delegate.js` (gated: `npm run e2e:delegate`, added to `ci.yml`) drives real Chrome: select `demo_delegate` → I/O → assert the `.branch[data-child='demo_delegate_child']` → click → land in the child (its `FinalAnswer`) → assert the `◂ parent` crumb → click back. PASS. perceptual — `harness/capture_delegate.js` element-screenshots the bounded I/O pane; both frames VIEWED: the parent shows `↳ delegated child: demo_delegate_child` (cyan link), the child shows `◂ parent: demo_delegate` + its own calculator record — legible, on-theme, tone canon holds (lowercase, no emoji). **Dual contract:** `node --check web/app.js` clean; 30 server tests pass (server.py touched); all four e2e green together (e2e / e2e:studio / e2e:assay / e2e:delegate). **Rubber Duck Pass:** *Sequence* — parent I/O lists the ToolResult artifact; `_resolve_child_name` matches `child_root`→`demo_delegate_child`; the branch link fires `selectRecord` + sets `delegateParent`; the child view renders the crumb; the crumb clears `delegateParent` + reselects the parent. *Observations (six):* missing pair — none; order — the crumb only shows after a branch navigation (rail picks clear it); vocabulary gap — none (reads substrate's kinds, no UI vocab); payload anomaly — the ToolResult artifact still gists as `output=[object Object]` (a pre-existing `gistPayload` limit for object payloads; the branch below it carries the meaning — noted, not W2.2's scope); timing surprise — none; tone trace — copy is lowercase textual markers, on-canon. *Disposition:* one surfaced (the `[object Object]` gist — a `## Drift watchlist` candidate), rest clean. **Honest limit:** the demo child is servable so the branch is navigable; a REAL cockpit delegate child lands in the session workspace (unserved) and renders display-only until live cockpit-delegation writes children into the served dir — the named follow-on. **NEXT:** live cockpit delegation (delegate in the `/api/agent` suite + child records under a served dir), or substrate phase W3 (MCP).

- **2026-07-31 — Sprint 016 (RETROACTIVE, review F-27): terminal dock-head polish + driver-picker theming — CLOSED.** Three commits shipped after sprint 015 with no card/entry/diary line, one reversing another; this backfills the ledger. `e3f9175` styled the driver `<select>` to the console theme (`.term-model`). `81c8f2d` made the dock head responsive (`flex-shrink`, `min-width:0`, ellipsis — degrade-don't-wrap). `49af067` REVERSED it to rigid (`.term-head > *{ flex-shrink:0; white-space:nowrap }` — never shrink, clip at the edge) after the user, watching the running console, saw the shrink "jump" and read it as a glitch, not adaptation; plus a workflow-parity backlog note in `process/BACKLOG.md`. Card: `sprints/sprint-016-terminal-dock-polish.md`. **Observation contract:** perceptual track (the right fixture for a "never wraps/shrinks" claim) — `screenshots/driver-styled.png` + `screenshots/head-{560,760,1000,1400}.png` VIEWED across four widths, the head clips without wrapping or shrinking; `screenshots/` is gitignored but the regenerating harness is in-repo, so it reproduces. Structural: `npm run e2e` (console) still PASS with the restyled head. **Also this session (F-28):** `harness/e2e_assay.js` — committed as an observation contract (sprint 014, the assay matrix: both currencies present, the metric-splice guard) but gated by NOTHING (absent from `package.json` scripts and `ci.yml`) — is now wired as `e2e:assay` and run in CI after `e2e`/`e2e:studio`; verified PASS against the live server (Addendum A7: an assertion outside the gate does not protect against regression). **Rubber Duck Pass:** *Sequence* — the two design commits are opposite (responsive vs rigid); the record now shows both with the reversal reasoned, not a silent contradiction. *Observations (six):* missing pair — the empty e2e_assay gate, now closed; order — n/a; vocabulary gap — none; payload anomaly — none; timing surprise — none; tone trace — the card/entry hold the plain register. *Disposition:* clean; two ledger gaps (the uncarded commits, the ungated contract) closed. **Lesson (KIT_DIARY):** code-first on "small" UI items is the same trap review #39 named — a visual tweak still needs a card, and a committed contract still needs a gate.

- **2026-07-30 — Sprint 015: call parameters visible + settable in the agent terminal — CLOSED.** The follow-through on the token-cap finding: the parameters a turn runs with are now first-class. `server.py` — `_agent_params(q)` parses `think` / `max_tokens` (0 = uncapped) / `timeout` from the request; applied to the Ollama responder (timeout to the CLI responder too, floored at 600); echoed back as `params` so the terminal shows what the server actually applied, not what the client assumed. `web/` — a params strip in the dock head (`think off · tokens ∞ · timeout 300s`) rendered by `renderTerm`; commands `think on|off`, `tokens N`, `timeout N`, `params` (bare, or /-prefixed in chat); the chat POST carries all three. **Dual contract:** `node --check` clean; 30 server tests incl. the `_agent_params` unit + the `/api/agent` params-echo assertion. **Observation contract, both tracks:** structural — e2e §15b asserts the default strip and that `tokens 4096` updates it (full e2e PASS); perceptual — `screenshots/agent-terminal.png` VIEWED: the strip sits between the driver picker and the mode hint, legible, showing the defaults after the reset. **Honest note:** the echo reports parsed values on every driver; only the Ollama branch consumes think/max_tokens (the CLI consumes timeout; deterministic consumes none) — per-driver applicability display is the parameters-pane direction in BACKLOG. NEXT (BACKLOG, 2026-07-30): the parameters pane (per instantiation, per gateway); thinking capture once the engine records it.

- **2026-07-30 — the agent seam un-starved: no token cap, timeout 300, max_steps 8→24.** The live chat's models looked broken (deepseek-v4-pro "announcing" a script every turn, never writing it) because `_agent` constructed a bare `OllamaResponder` — inheriting the 512-token default — while the agency assay that ranked those models ran at 8192. Fixed with substrate's class-default change (no cap: `num_predict` omitted; bounded by num_ctx + timeout), plus seam-side `timeout=300.0` on both Ollama construction sites and `max_steps` 8→24 (8 was a demo budget; the wrap-up trigger still ends every run on a FinalAnswer). Verified end-to-end through the seam itself: `POST /api/agent` → record `launch_agent_deepseek-v4-pro_93ef31043bdb` — write_file → read-back → bash exit 0 → FinalAnswer with the real output; the artifact runs standalone. 29 server tests green; server restarted. Full diagnosis + the defaults census on substrate's board (same date).

- **2026-06-26 — Sprint 014: the arm-matrix VIEW (frontend) — CLOSED.** Renders sprint 013's data seam: a new **ASSAYS** rail group (prepended above RUN RECORDS after the records fill) and a center **arm matrix** at the above-a-run altitude. `web/index.html` — `.assay`/`.am` CSS, the margin-verdict colors, the provenance header, an `#assaypane` beside `#readpane`/`#iopane`. `web/app.js` — `loadAssays()`, `selectAssay()` (fetch+cache+render, staleness-guarded like `selectRecord`), `renderAssayFrom()`; `render()` flips to assay mode (shows `#assaypane`, HIDES the run-scoped cursor / health / health-verdict — meaningless at this altitude, hidden not repurposed); `selectRecord` restores the chrome on the way out. **The review's invariants are DISPLAY RULES:** both currencies always rendered (reliable pass^k AND per-trial pass@1 + a flake column) so the metric-splice is structurally unrenderable; the margin-verdict colored (inferior=red, equivalent=green, inconclusive=amber) WITH the CI, and the note states "significantly worse (CI excludes 0) ≠ inferior (CI clears −margin)"; provenance pinned; null→"—"; control marked "the bar". `server.py` — an mtime-keyed `_ASSAY_CACHE` (build_report's 5000-sample bootstrap is ~8s; a DONE run computes once then serves in 0.014s; a LIVE run recomputes only when its file grows). **Dual contract:** `node --check web/app.js` OK; server up. **Observation contract (both tracks):** structural — `harness/capture_assay.js` drives real Chrome, finds the ASSAYS group, clicks `coding_cells`, asserts 4 `[data-arm]` rows (strong_ref, weak_single, ensemble_no_correction, full); perceptual — `screenshots/40-assay-matrix.png` VIEWED: full = reliable 48/71 (0.676), per-trial 0.861, flake +0.185, Δreliable −0.254, Δpass@1 −0.115 [−0.182,−0.056] **inconclusive** (amber), weak_single + ensemble **inferior** (red), control marked, the note legible, cursor/health hidden — clean, on-vocabulary. Server LEFT UP on :8765 so the human opens it too. NEXT: drill a cell → the existing run-observer (each cell IS a record); the live `coding_coder` arm matrix once its `full` arm completes.

- **2026-06-26 — Sprint 013: the assay read-projection (backend) — CLOSED.** The console reads ONE record; an **assay run** is a different altitude — many records (arms × cases × trials) read as one arm comparison (the firewalled coding A/B, the ablation ladder). Added the data seam. NEW canonical home `../substrate/src/substrate/assay/cells.py` (`read_meta`/`read_rows`/`caseresult_from_row`/`suite_from_meta`/`report_from_cells`) so the CLI `bench_coding.py report` and this server share ONE "cells file → Report" impl (no drift): reconstructs CaseResults from the JSONL rows (null compute → 0 for salvage/fail cells) + the Suite from the `.meta.json` + the pre-registered coding bank, then the same `build_report`. `server.py`: two read-only GET projections — `/api/assays` (scan `bench_results/*.jsonl` + sidecar → name, fingerprint, models, margin, trials, n_cells, arms) and `/api/assay/<name>` (`report_from_cells` → the arm matrix: per-arm reliable pass^k AND per-trial pass@1 AND flake, the harsher `delta_vs_control` + McNemar p, the pass@1 Δ + CI + `margin_verdict`, FDR — both currencies, never spliced). The assay's read is NOT the console's run-HEALTH "verdict"; named `margin-verdict` to avoid collision. **Dual contract:** mypy --strict (74 files) + ruff clean (both repos); 17 assay tests pass. **Observation contract (backend — curl, the data seam):** `curl /api/assays` lists BOTH `coding_cells` (the 480B run, 2840 cells, pre-provenance → reconstructs from defaults) AND the LIVE `coding_coder` (fingerprint `3c606201fdae`, 480B vs the coder ensemble, 2655 cells mid-run, all 4 arms); `curl /api/assay/coding_cells` returns `full` = reliable 48/71, pass@1 0.861, flake +0.185, Δreliable −0.254, Δpass@1 −0.115, margin-verdict `inconclusive`, fdr true — EXACTLY the numbers `bench_coding.py report` prints (same `build_report`, same data). NEXT (Sprint 014): the arm-matrix VIEW in `web/` (rail group beside RUN RECORDS + the matrix render, both currencies + the honest-verdict invariants) + the harness PERCEPTUAL capture (screenshot the matrix against real `coding_cells`).

- **2026-06-22 — Sprint 012 (item A): glider fixture closes the pixel-decode mirror-blind spot — sprint 012 CLOSED.** Review #50 noted `capture_scene`'s pixel decode catches transpose but NOT a pure L-R/U-D mirror render bug, because the blinker is mirror-symmetric (maps to itself under mirroring). Added (substrate `20b70cd`) a `game_of_life_glider` bundled topology — an ASYMMETRIC, MOVING glider (7×7, generations=4). `capture_scene` now decodes BOTH the blinker (V/H/V) AND the glider (5 generations, seqs 3→999), and asserts each glider frame's decoded grid != its own L-R mirror — so a mirror render bug WOULD diverge here (it can't on the symmetric blinker). Refactored the decoder into `openScene`/`decodeFrame` helpers. **Found + fixed a real harness race**: `openScene` read `STATE.scene` before the record finished loading (`selectRecord` sets `STATE.name` at :193 but `STATE.scene` at :208), so it got the PREVIOUS record's seqs — fixed by fetching the ground-truth Generation seqs from the record API + waiting until `STATE.events` matches the record's max seq. Both tracks: structural (`npm run capture:scene` PIXEL-DECODE PASS: blinker 3 gens + glider 5 gens all decode-match; `glider_mirror_sensitive=true` every frame) + perceptual (`screenshots/scene_glider_gen0.png` VIEWED: the textbook glider, asymmetric + clean). Closes the mirror-blind Drift item — **sprint 012 (B rAF, C grouping, C2 prune, A glider) DONE**. Only `noted` drift item left: D (fold capture_scene into the canonical harnesses).

- **2026-06-22 — Sprint 012 (item C2): prune affordance (clear your runs) + corrected source tag + cleaned detritus — runs/-lifecycle Drift item CLOSED.** The grouping (item C) corralled session runs but 231 had accumulated, burying the demos. Added a prune: server `POST /api/runs/clear` deletes ONLY the hash-suffixed session runs (`launch_`/`build_`/`resume_` — `_SESSION_PREFIXES`); bundled demos + the named `demo_*` FIXTURES are KEPT; live in-flight records skipped; CSRF-guarded like `/api/launch`; returns `{removed, kept}`. A cyan **clear** affordance in the your-runs group header (confirm-gated) POSTs it + reloads. ALSO corrected item C's `source` tag — it used `name in bundled.names()`, which wrongly tagged the `demo_*` fixtures (demo_failed/paused/broken/solo_chat/diff/resumable/torn) as "your runs"; now `name.startswith(_SESSION_PREFIXES)`, so the 8 fixtures correctly group under "demos". EXPLICIT user action (confirm-gated) — not a silent clobber, so review #35's no-SILENT-deletion durability ruling holds. Cleaned the live detritus (`clear` returned `{removed:231, kept:8}`). `server.py` (`_clear_runs` + dispatch + `_SESSION_PREFIXES` + the corrected tag) + `web/app.js` (the clear button) + `web/index.html` (`.rail-clear` CSS) + `tests/test_server.py` (a clear test). Both tracks: structural (`test_server.py`: clear prunes the session run, keeps every demo+fixture, launch still works; e2e: "your runs · N clear | demos" + the clear affordance present; **25 server tests**) + perceptual (`screenshots/rail_clear.png` VIEWED: the cyan "clear" in the YOUR RUNS header). Closes the runs/-lifecycle Drift item. NEXT (item A): the glider fixture for the mirror-blind pixel decode.

- **2026-06-22 — Sprint 012 (item C): runs/ rail grouping — demos vs your-runs (Drift watchlist fold).** The rail enumerated all records flat (bundled demos + accumulating runs/ session records), so transient runs buried the demos (239 runs vs 11 demos on the live server). Now the server tags each record `source=demo|run` (`_records_index`: `name in bundled.names()`), and the client groups **"your runs · N"** (the runs/ records, newest-first by ULID `run_id` — ULIDs are time-sortable) ABOVE the stable **"demos"** fixtures, with `.rail-group` headers. Default landing = the newest run (else first demo). `server.py` (the `source` field) + `web/app.js` (the `mkRec`/`groupHdr` grouping in `loadRecords`; `ordered[0]` landing) + `web/index.html` (`.rail-group` CSS). Both tracks: structural (`e2e_console.js`: rail shows "your runs · N" + "demos" group headers; 24 server tests pass — `source` is additive) + perceptual (`screenshots/rail_grouped.png` VIEWED: "YOUR RUNS · 239" with session runs newest-first, demos below, on-vocabulary). Sprint card `sprints/sprint-012-drift-polish.md` declares the cluster contract. NEXT (item C2): a prune affordance (clear your runs) — 239 accumulated runs need it; the grouping corrals but the prune clears.

- **2026-06-22 — Drift-watchlist fold: replay loop → requestAnimationFrame (render-rate decoupled).** The play loop was `setInterval(_tick)` calling a full `render()` per seq, so high speeds capped at render cost (~60/s) — render-bound (the sprint 009 drift item). Now a `requestAnimationFrame` loop advances the cursor by (elapsed × speed) seqs and renders ONCE per frame, so the replay RATE is decoupled from the ~60 fps render rate (480/s advances ~8 seqs/frame, one render). Restored honest higher speeds (selector now **4/30/120/480 seq/s**, default 30; the review #50 relabel to a 60-max is superseded — these are now genuinely attainable, not fiction). `web/app.js` (the `_frame`/`startPlay`/`stopPlay` rAF rewrite; `setInterval`→`requestAnimationFrame`) + `web/index.html` (speed options; dropped the "render-bound near the top" title). NO backend change. Both tracks: **structural** (`e2e_console.js` §14 now plays a 200-seq TAIL at 480/s and reaches max 263==263 in a 1.5 s window the old ~60/s loop physically could NOT clear — proving the decoupling, not just stop-at-end; rewind lands near 0; pause holds) + **perceptual** (`screenshots/replay_raf.png` VIEWED: ⏮ ⏸ green-while-playing + slider mid-replay; speed options confirmed 4/30/120/480). Closes the render-bound drift-watchlist item.

- **2026-06-22 — Sprint 011: application content views (the "see the code" cluster).** Closed the two BACKLOG content items. **(1) Readable content in the inspector:** `inspectEvent` now renders string payload fields that are code/prose/model-output (contain newlines or ≥40 chars) as a dedicated CONTENT block — real newlines, monospace, green left-border — above the raw payload JSON. So clicking a pair_coding `CodeChunk` shows `def solve(x):` as actual readable code (not buried in escaped JSON); a conversation `Turn` shows the turn text. **(2) Inspector works on output artifacts:** each output artifact row in the I/O pane is now clickable (cursor + hover) -> `inspectEvent(seq)`, filling the inspector with its full content — same as a stream event. `web/app.js` (inspectEvent content blocks; renderIO clickable artifacts) + `web/index.html` (`.insp pre.content`, clickable `.art`). NO backend change. Dual contract: artifact (`node --check` clean; eight-word grep clean; 24 server tests unaffected) + observation BOTH tracks — **structural** (`e2e_console.js` §16: clicking a pair_coding CodeChunk shows a content block with the real code `def solve`; clicking an output artifact inspects it) AND **perceptual** (`screenshots/inspector_content.png` captured AND VIEWED: TEXT block renders `def solve(x):` readable with a green border, above the payload — clean). Rubber Duck Pass (## Sprint tail). Closes the two BACKLOG content items. NEXT (per the Architect's reorder — foundation before house): the `tool_loop` best-in-class real tool suite (un-park `../substrate/docs/tool-loop-tool-suite.md`), then drift-watchlist polish; the interactive agent waits for a usage/theory pass.

- **2026-06-22 — Run-as-graph legibility: split each lane into queued | ran (Architect-flagged).** The firing-anchored lane bar (`fired_seq` -> `ended_seq`) with the spawn dot at `started_seq` read as "spawned at the end of a solid green bar" — nonsensical. **DIAGNOSED by correlating `run_graph` with the screenshot** (the SDD discipline — read the state, don't guess): game_of_life cells fire early (5,7,9…) but start LATE (55,58,61…) and run ~2 seqs (ended 57,60,63…), so `started_seq` sits at ~96% of the fired->ended bar — i.e. 94% of the bar was the cell WAITING in the single-writer admission queue, ~4% the actual run. **FIXED:** each lane now renders two segments — a faint hatched `fired->started` (scheduled/queued) + a solid status-coloured `started->ended` (ran) — with the spawn dot at the boundary (the run START). Legend relabeled (scheduled/queued · ran->completed · started). `web/app.js` renderGraph + `web/index.html` (.qbar/.leg-q CSS). Verified BOTH tracks on game_of_life as known ground truth: **structural/mechanical** — a Playwright correlation read each rendered lane's qbar/bar/dot geometry and asserted it against `run_graph` fired/started/ended: **53/53 lanes correlate, 0 mismatches, dot==run-start for every started lane** (gated in `e2e_console.js` §2) — AND **perceptual** (`screenshots/graph_segments.png` VIEWED: faint-queued + solid-ran + dot-at-boundary; the staircase now legibly "each cell queued progressively longer, ran briefly"; world lanes mostly-solid). Closes the BACKLOG spawn-dot legibility item.

- **2026-06-22 — Sprint 010: the integrated terminal (read interface) + seq/time pairing.** Ported the designed terminal dock (`handoff/console-v2/ui_terminal.jsx`) the console never shipped — the surface that closes the "I can't see the output / the code" gap. A collapsible dock (▌ button + Ctrl+`) with a read interface over the SAME record the GUI shows: `tail [--kind --producer --all]`, **`cat <seq>` (the full payload of an event — the actual code / turn text / model output)**, `ls` (output events), `input` (the seed), `narrate` (the legible causal beats), `inspect <kind|instance>` (provenance), `clear`; Up/Down history. Reads `STATE.events` + the existing `/io` + `/explain` endpoints — no backend change, no control, no model (the READ half; the interactive model agent is the next sprint). **Plus the Architect's seq/time pairing:** every event list now shows the relative timestamp `t+Δs` alongside seq (seq = order without time; t = time without order; seeing both is the picture) — in the event stream, the terminal `tail`/`narrate`/`cat`, and the inspector (with the raw epoch). ~0 on the deterministic CI demos (honestly: instant), real per-turn gaps on a real-model run. `web/index.html` (dock + CSS + the stream `.tt` column) + `web/app.js` (the command interpreter + `relT()` + dock render/history/toggle). Dual contract: artifact (`node --check` clean; eight-word grep clean — terminal/tail/cat/narrate/inspect are command verbs, not primitive renames; 24 server tests unaffected) + observation BOTH tracks — **structural** (`e2e_console.js`: open the dock, `cat <appseq>` on pair_coding prints the CodeChunk full payload incl. `"text"`/`solution.py`; `narrate` prints beats incl. RunFinalised + `t+`; the stream carries `t+`) AND **perceptual** (`screenshots/terminal.png` captured AND VIEWED: the dock renders the color-coded narrate with seq + `t+Δs`, prompt `pair_coding$`, clean + on-vocabulary). Rubber Duck Pass (## Sprint tail). Also started `process/BACKLOG.md` — the Architect's running notes (inspector-on-output-artifacts; richer application-code views; the run-as-graph spawn-dot legibility; the agent-IDE research direction). NEXT: the interactive open-source-model agent in the terminal.

- **2026-06-22 — Review #50 fold (replay Transport + scene/decoder).** Review #50 PASSed both increments (no timer leak / exact stop-at-max / working rewind; the pixel decoder verified GENUINE — it decoded three DISTINCT V/H/V patterns from real screenshot pixels, so it cannot be echoing the target; the fold genuinely gates in CI; the canon's own grep run clean, closing the #49 open item). Folded the low items: **(1, honesty)** the speed selector overpromised — the `max(16, 1000/speed)` floor clamps both 80/s and 240/s to ~62/s, so "80/s" ran ~62/s and 240/s was fiction; relabeled to attainable rates (4/12/30/60 seq/s, default 30); **(2)** `game_of_life` now rejects a jagged (non-rectangular) `initial`, mirroring the UI's `isGrid` tightening; **(3)** named `capture_scene`'s bound in its header (render-fidelity, NOT end-to-end record verification — the pytest + e2e index assertions cover model-correctness). Deferred **(4)** a glider fixture for mirror coverage (the blinker is mirror-symmetric; transpose IS caught, a pure mirror is not) -> Drift watchlist. Verified: `npm run e2e` PASS (play 0->21 at the 30/s default, stops 263==263, rewind 28), `npm run capture:scene` PIXEL-DECODE PASS (V/H/V), game_of_life 2 tests + the guard (empty / empty-row / jagged all rejected; default builds). NEXT: the terminal (cat/narrate/tail) -> the interactive open-source-model agent.

- **2026-06-22 — Sprint 009: the replay Transport (play / pause / speed / rewind).** Ported the designed Transport (`handoff/console-v2/app.jsx`) the shipped console had dropped for a static slider: a ▶/⏸ play button + a speed selector (8/24/80/240 seq/s) in the cursor bar drive a play-loop that advances the ONE seq-cursor over time, so replay animates the graph + stream + scene in lock-step (no new time axis). Reaching the end STOPS; pressing ▶ at the end REWINDS to 0 and replays; grabbing the slider or switching records pauses (no loop leaking across records). `web/index.html` (controls + CSS) + `web/app.js` (`STATE.playing`/`speed`, `startPlay`/`stopPlay`/`_tick`, the wiring, `stopPlay()` in `selectRecord`). NO backend change. Dual contract: artifact (`node --check` clean; eight-word grep clean — play/replay/speed are control verbs, not primitive renames; 24 server tests unaffected) + observation BOTH tracks — **structural** (`e2e_console.js` reads the cursor STATE `#seqnow` as the signal: play advances 0->44, pause holds 44, play stops at the end 263==263, the button returns to ▶, replay from the end rewinds to ~0 (seq 28)) AND **perceptual** (`screenshots/replay_controls.png` captured AND VIEWED: the ▶/⏸ renders green-while-playing, the 80/s selector + seq 45/263 mid-replay, clean + on-vocabulary). Rubber Duck Pass (## Sprint tail). Honest note: the per-tick `render()` makes the top speed render-bound (~render cost, not the nominal 240/s) — fine for the demos; a `requestAnimationFrame` / skip-render-on-fast pass is a future optimization (Drift watchlist). Original-timing replay (via the events' `t`) deferred — near-instant on the deterministic CI records, meaningful for real-model runs. NEXT: the terminal (read commands cat/narrate/tail) -> the interactive open-source-model agent.

- **2026-06-22 — Review #49 fold + deterministic-pixel-anchor decode for the scene.** Review #49 PASSed both increments (game_of_life correct on every sub-question; scene lens-pure; no XSS) with one real corner-cut: the scene's STRUCTURAL assertions lived only in `capture_scene.js`, which CI never runs. **Folded:** moved the scene's structural assertions into the CI-gated `harness/e2e_console.js` (`npm run e2e` — tab show/hide by shape, 25 cells, the blinker's live-cell indices move gen0->gen1); applied the two low-severity folds (`isGrid` now requires RECTANGULAR rows — kills jagged misrender + lowers the incidental-matrix hijack risk; `game_of_life` raises a clear `ValueError` on an empty `initial`); re-homed `capture_scene.js` into `harness/` with a `capture:scene` npm script (was orphaned at the repo root). **Then, per the Architect (use the actual SDD visual-verification technique — deterministic pixel anchors, not eyeballing): rebuilt `capture_scene.js` as a real pixel-decoder.** It screenshots the rendered `.scene-grid`, decodes the PNG with Node's built-in `zlib` (no deps), reads the actual pixel at each cell's centre, classifies alive (green channel dominant) vs dead, reconstructs the grid, and ASSERTS it equals the record's real `Generation.grid` — emitting the decoded grid as ASCII + a `match=` signal the agent READS (a state snapshot), not a PNG it eyeballs; on mismatch it prints decoded-vs-expected (assertion-with-context). PNGs still written for the polish look (two-track: decode = mechanical half, look = vision-model-judge half). Verified: `npm run e2e` PASS incl. the scene section; `npm run capture:scene` PASS (gen0 vertical / gen1 horizontal / gen2 vertical — every decoded grid matches the record); game_of_life tests 2 passed + the guard; node --check + eight-word grep clean. NEXT (held): the replay Transport (play/pause/speed) + the terminal.

- **2026-06-22 — Sprint 008: the scene panel (custom visual output for applications).** Added a third graph view — **scene** — beside run-as-graph and structure: a domain-visual rendering of a renderable payload shape, for topologies whose output is visual (the Architect raised this when `game_of_life` shipped in the parent repo — its `Generation.grid` MEANS a board of cells, which the run-as-graph / event stream render the substrate's shape of, not the domain's). **Opt-in by SHAPE, not by app code** (the lens stays pure): `findGrids` scans the record's event payloads for the first 2-D numeric array and renders it as a cell grid; `game_of_life` qualifies via `Generation.grid`, and any board / heatmap / matrix topology qualifies for free — no per-app renderer, no coupling, read-only over `STATE.events`. **Cursor-driven:** shows the latest renderable frame at or before the seq-cursor, so scrubbing animates the generations in lock-step with the graph + stream (reuses the one-cursor architecture; no second time axis). The tab appears ONLY when a renderable shape exists (hidden for `code_review` etc.). `web/app.js` (`findGrids`/`renderScene`/`updateScene` + the `gvScene` toggle + `STATE.scene`) + `web/index.html` (the `#gvScene` tab + `.scene-grid`/`.cell` CSS). NO backend change. Dual contract: artifact (`node --check web/app.js` clean; eight-word grep clean — scene/cell/grid are not primitive names; 24 server tests unaffected) + observation BOTH tracks — **structural** (`capture_scene.js`, real Chrome: scene tab visible for `game_of_life` + hidden for `code_review`; clicking renders a 5×5 grid = 25 cells with 3 live; scrubbing to each `Generation` seq changes the live-cell positions) AND **perceptual** (3 frames captured to `screenshots/scene_gol_gen{0,1,2}.png` AND VIEWED: the blinker renders as a 3-cell green bar and oscillates **vertical → horizontal → vertical** as the cursor steps seq 3 → 132 → 261; layout clean, on-vocabulary, no overflow). Sprint card declared the dual + observation contract BEFORE code (`sprints/sprint-008-scene-panel.md`). Rubber Duck Pass clean (## Sprint tail). Honest scoping note: the structural assertions + the screenshots both live in one new `capture_scene.js` rather than split across the canonical `e2e_console.js` / `capture_console.js` — folding them in is a follow-up (Drift watchlist). NEXT: the duplex-pipe review of the scene panel.

- **POINTER — rounds #30–#38 (UI build-out, 2026-06-15..16).** The observe+control console was built and folded across these rounds BEFORE this board existed. Their record:
  - Review envelopes: `../.review-pipe/resp-030.txt` … `resp-038.txt` (each finding folded + re-verified).
  - The 5 "UI BUILD step N" entries currently in `../substrate/BLACKBOARD.md ## Built` (a category leak into substrate's board; to be physically relocated here at the next substrate housekeeping pass — review #39 finding 4, low urgency).
  - Net shipped #30–#38: record rail + run-as-graph + event stream + provenance + health verdict + diff + I/O pane (#30–#34); thin control — launch (#35), live-attach + torn-record handling (#36–#37), resume + the "interrupted" status + the NOT-CLEAN-flicker fix (#38). All under `test_server.py` (18) + `e2e_console.js` (live).

- **2026-06-17 — Artifact-discipline retrofit (review #39 fold).** Gave substrate-ui its own git repo (`git init -b main`, `.gitignore` for generated `runs/*.record` + bytecode, baseline commit `cbdd22e` capturing the #30–#38 green tree). Instantiated the three core kit artifacts: this `BLACKBOARD.md`, `WORKING_AGREEMENT.md` (class web+backend; canonical home registry; `substrate.api`-only dependency boundary; the eight-word tone canon + its standing grep), `KIT_DIARY.md` (commit `3af7b52`). No own `signals/` vocabulary (the UI reads substrate's v0.2; a second lock would be ceremony — review #39 ruling). Dual contract: artifact (the files exist + parse) + signal (vacuous — content sprint). Closes review #39 finding D.

- **2026-06-17 — Sprint 007: Studio authoring for model-backed Producers.** Exposed the sprint-006 seam in the Studio: a `model` checkbox + a `prompt` field per Producer row, and a responder `<select>` (deterministic (CI · pure) default | ollama (real LLM)) by the build action. `buildSpec()` emits `producer.model`/`prompt` + `spec.responder` — exactly what `build_from_spec`/`_responder_for` accept. `web/studio.html` + `web/studio.js`. Dual contract: artifact (2 files; node --check clean; 22 server tests unaffected) + observation BOTH tracks: structural (`e2e_studio.js` — every row has a model checkbox; a responder selector present; marking judge model + prompt → build → the run's Verdict carries the Responder's output `stub[0]:…`, NOT "judge" — a model-backed run that executed) AND perceptual (frames 35/35b captured AND VIEWED — model checkbox + prompt per row, responder selector by the build, legible, on-vocabulary). Rubber Duck clean (below). **The E2 "full parity" Studio is now functionally complete: visual (canvas) + structured (form) authoring of Producers (stub or model) / Views / Triggers / Routes / composed Termination + responder → real validate + build → view.** NEXT: send the drag-canvas + model-Producer batch (sprints 005–007) to the duplex-pipe reviewer (Architect's request).

- **2026-06-17 — Sprint 006: model-backed Producer seam (real Responder, deterministic-testable).** Lifted the stub's "emits note=kind" ceiling: a Producer with `model: true` (+ `prompt`) now calls the runtime's REAL `Responder.respond(prompt)` and emits its kind carrying the response. `build_from_spec(spec, responder=None)` defaults to the runtime's `DeterministicResponder` (CI mode — pure, seeded, no network, replay-stable); `server.py` `_responder_for(spec)` selects `deterministic` (default) | `ollama` (`OllamaResponder`, real LLM, loud failure if absent — never a silent fake). This is the runtime's own pattern (topologies written against `Responder`; the mode is which Responder the run is handed). A model Producer is declared `deterministic` iff its Responder is. `builder.py` + `server.py`. Dual contract: artifact (2 files; ruff clean) + observation (`test_build_model_producer_runs_the_responder`: a model topology built with DeterministicResponder → the emitted payload `note` equals `DeterministicResponder(seed=0).respond("rate this")`, NOT "rater" — the responder genuinely ran). 22 server tests pass. Rubber Duck clean (below). The Studio authoring UI for `model`/`prompt`/responder is sprint 007 (front-end, two-track).

- **2026-06-17 — Sprint 005: Studio drag-canvas (visual node-graph).** The visual half of the E2 Studio: a form↔canvas toggle + `renderCanvas()` that reads the live `buildSpec()` and draws Producers as draggable cards (initial = green border + ▸ badge) and Triggers/Routes as curved SVG edges (solid/dashed, arrow-headed, labelled). Hand-rolled SVG + drag, NO library, no backend change. `web/studio.html` + `web/studio.js`. Dual contract: artifact (2 files; node --check clean; 21 server tests unaffected) + observation BOTH tracks: structural (`e2e_studio.js` — 3 cards, 2 initial, 2 `adjudicate` edges, drag moved a card, form round-trips intact + still validates) AND perceptual (frame 34 captured AND VIEWED — reviewer-a/reviewer-b green-initial cards + judge, two curved `adjudicate` arrows reviewers→judge, clean layout, no overlap, on-vocabulary). Rubber Duck clean (below). The canvas is a VIEW (form is the source of truth); canvas-based CREATION + real-model Producers are the last full-parity pieces.

- **2026-06-17 — Sprint 004: Studio Routes + any_of/all_of termination composition authoring.** Exposed the two authoring primitives the form-first MVP left out but `builder.py` already translates: a ROUTES section (id / of / slot → `spec.routes[]`) and composable TerminationPolicy (any_of / all_of with authored member rows → `spec.termination.members[]`, each all_completed / quiescence_with_watchdog(seconds) / threshold_count(of,n) / cancel_all_others). `web/studio.html` + `web/studio.js` only; the assembled spec stays EXACTLY `build_from_spec`-accepted (confirmed by a direct validate+build probe: route+all_of → valid → finalised). NO backend change. Dual contract: artifact (2 files; node --check clean; 21 server tests unaffected) + observation BOTH tracks: structural (`e2e_studio.js` extended — all_of shows 2 default members; Route + all_of composition → valid → built · finalised) AND perceptual (frame 33 captured AND VIEWED — ROUTES + composed-termination members render cleanly, coherent with the form, on-vocabulary). Rubber Duck clean (below). Remaining "full parity" gap: a drag-canvas (cosmetic over the same spec) + real-model Producers (lift the stub emit-once ceiling).

- **2026-06-17 — Sprint 003: Studio authoring surface (form-first, real build & launch).** The E2 Studio's core author→validate→build→view loop. `web/studio.html` + `web/studio.js` (served at `/studio.html` by the existing static handler — NO backend change): structured authoring of Producers (kind/emits/initial), Views (name/kind/of), Triggers (id/on/predicate view-op-n/starts/policy), TerminationPolicy; assembles the authored-spec JSON mirroring `builder.py` EXACTLY; "validate" → `/api/validate` (real `TopologyBuilder.build()`); "build & launch" → `/api/build` (real `api.Runtime`), shows status + `unfired_triggers` + a `/?record=` deep-link. `web/app.js` honors `?record=` on load. Dual contract: artifact (4 files; node --check clean; 21 server tests unaffected) + observation BOTH tracks: structural (`e2e_studio.js`, 8/8 — validate→valid, build→finalised, deep-link selects it, the authored run EXECUTED [Critique→adjudicate fired→Verdict], unknown-starts→clean typed invalid) AND perceptual (frames 30/32 captured AND VIEWED — clean legible authoring form + build result, on-vocabulary, no overflow). Rubber Duck Pass clean (below). The form-first MVP of the E2 "full parity" Studio; DEFERRED to later sprints: Routes + any_of/all_of nested-composition authoring, a drag-canvas, real-model Producers.

- **2026-06-17 — Sprint 002: static topology-structure view (closes the design-§6 read-side gap).** Wired the previously-unrendered `/topology_graph` endpoint into the console as a toggle beside the run-as-graph (`web/index.html` header toggle "run-as-graph · structure" + CSS; `web/app.js` `renderTopology()` reading `STATE.topology`, fetched in `selectRecord`). The STRUCTURE view renders the authored topology — Producers (initial-marked) + emitted kinds, Triggers (id/on/starts/policy), Views, Routes, TerminationPolicy — seq-independent, visually distinct from the run timeline, on the eight words. Dual contract: artifact (app.js + index.html; node --check clean; 21 server tests unaffected) + observation BOTH tracks: structural (E2E green incl. new assertions — authored Producers initial-marked + adjudicate Trigger; 0 lanes in structure; 6 lanes restored on toggle-back) AND perceptual (frame 02b captured AND VIEWED — legible, distinct, on-vocabulary, no overflow). Rubber Duck Pass: clean (below). Closes the verification-surfaced gap; the console now ships BOTH design-§6 graph surfaces.

- **2026-06-17 — Wider perceptual re-verification (`capture_states.js`).** Added the dynamic/edge capture pass (`npm run capture:states`): 6 frames — torn (amber INCOMPLETE), event-click inspector, seq-0 edge, live ● LIVE pulse, live→FINALISED, resume→FINALISED+interrupted-bar. Captured + VIEWED all 6: every dynamic state renders with correct semantic colors and the inspector-clear fix holds throughout; no new stale-class bugs. Surfaced one MED gap (`/topology_graph` built+tested but unrendered — the static topology-structure view the design §6 promised is missing from the console; Architect ruling pending) + 2 LOW minors (drift watchlist). Dual contract: artifact (capture_states.js + `capture:states` script) + observation (the 6 screenshots, viewed). Transient launch_/resume_ records cleaned from runs/ after.

- **2026-06-17 — Perceptual observation track built + run; stale-inspector bug fixed (Architect-caught).** Built `capture_console.js` (`npm run capture`, repo-scoped Playwright, `channel:'chrome'`) — the SECOND, perceptual track of the two-track visual grading. Captured 11 key frames and VIEWED each: the console renders correctly (semantic verdict colors red/cyan/red/green; firing-anchored run-as-graph bars + spawn-cohort bands; the natural_conversation staircase; honest on-vocabulary bottom-status). The look-pass found a real bug the DOM E2E missed — the inspector + diff pane did NOT clear on record switch (stale `judge` provenance / `demo_diff` diff bled across records). Fixed `web/app.js` `selectRecord` (reset `#insp` to its placeholder + clear `#diffsel` on every switch); added the E2E regression assertion "inspector clears on record switch"; re-captured + re-viewed — both stale frames now show the placeholder. Cleaned 50 transient test records from `runs/` (rail 53→17, defaults to a real demo). Updated WORKING_AGREEMENT to MANDATE both tracks for front-end changes. Dual contract: artifact (capture harness + package.json `capture` script + app.js fix) + observation (E2E 22→ now with stale-clear guard, green; the 11 screenshots, viewed). Closes the perceptual-track gap.

- **2026-06-17 — E2E harness scoped into the repo + the observation contract RUN (Architect-caught discipline fix).** Replaced the vanished `/tmp/pw-substrate` install with a repo-local, lockfile-pinned Playwright devDependency (`package.json` + `package-lock.json`; `node_modules/` gitignored; `channel:'chrome'` → system Chrome, no download). Ran the full live E2E against the real backend: **22 checks PASS, exit 0** (record rail, run-as-graph + cohort bands, provenance, the §7.2 failure verdicts, diff at seq 5, I/O + baseline, launch, live→finalised, torn→amber, resume→finalised-no-flicker, stage1 interrupted). This CLOSES the honest gap flagged in the sprint-001 entry below ("Live E2E NOT run this session") — it is now run and green, and reproducible by any session via `npm install && npm run e2e`. Dual contract: artifact (package.json/lockfile committed, docs corrected) + observation (the E2E itself, run). The discipline lesson recorded in KIT_DIARY.

- **2026-06-17 — Sprint 001: Studio build seam under test + made honest (review #39 findings 2+3).** FIRST increment under formal discipline (sprint card `sprints/sprint-001-studio-build-seam.md` declared its dual+observation contract BEFORE the code landed — the exact fix review #39 prescribed). Added the two translator tests the live seam was missing (`test_build_runs_an_authored_topology` — proves an authored topology EXECUTES: a triggered judge emits `Verdict` only because `adjudicate` fired on the `KindCount(Critique)>=2` quorum; `test_validate_accepts_good_rejects_bad` — good→valid, no-producers/unknown-starts→clean typed invalid). Closed the stub honesty edge: `build_from_spec` docstrings the emit-once count-ceiling, and `/api/build` now surfaces `unfired_triggers` (authored Triggers whose Predicate never matured) — tested by `test_build_surfaces_unfired_triggers` (an unreachable `KindCount(X)>=3` finalises green but names the unfired Trigger + emits no `Y`). Dual contract: artifact (test_server.py 21 pass; ruff clean on builder/server/tests) + observation (the authored build records assert the Trigger demonstrably fired). Live E2E NOT run this session (Playwright absent in this env; backend-only change, the E2E exercises no `/api/build` path) — honest gap, flagged. Closes review #39 findings 2 + 3.

---

## Deferred

*Anyone may append. Re-visit conditions noted.*

- **2026-06-17 — Relocate the 5 UI ## Built entries out of `../substrate/BLACKBOARD.md` into this board.** Low urgency (the pointer above suffices now); do it at the next substrate housekeeping pass so substrate's published board carries no UI-build noise. (Review #39 finding 4.)

---

## Open questions

*Anyone may append.*

- None outstanding.

---

## Drift watchlist

*Agent maintains. Patterns to monitor across increments.*

- **2026-08-03 (sprint 017, LOW) — `gistPayload` renders an object-valued output as `output=[object Object]`.** The I/O artifact one-liner for a `ToolResult` whose `output` is an object (the delegate result `{answer, child_root, steps}`) shows `[object Object]` — uninformative. W2.2's branch below it carries the meaning, so it is not misleading, just noise. Re-visit if more object-valued application outputs appear: `gistPayload` should summarise an object (key=val pairs) instead of `String(obj)`. Watch, not fix now.
- **[FOLDED 2026-06-22 — sprint 012 A] Pixel-anchor decode was mirror-blind on the symmetric blinker (review #50, LOW).** FIXED: added a `game_of_life_glider` bundled topology (asymmetric, moving); `capture_scene` now decodes it across 5 generations and asserts each frame != its L-R mirror, so a pure mirror render bug would diverge (the symmetric blinker couldn't catch it). Also fixed a harness race (read STATE.scene before the record loaded). ORIGINAL: `capture_scene` caught transpose but not a pure L-R/U-D mirror — the centred blinker maps to itself under mirroring.

- **[FOLDED 2026-06-22 — rAF] Replay Transport top speed was render-bound (sprint 009, LOW).** FIX: the play loop is now a `requestAnimationFrame` loop advancing (elapsed × speed) seqs and rendering once per frame — advance-rate decoupled from render-rate; honest higher speeds restored (4/30/120/480 seq/s); gated by e2e (a 200-seq tail at 480/s reaches the end in a window the old ~60/s loop couldn't). ORIGINAL: each play tick called `render()` (rebuilds graph+stream+scene), so the nominal high speeds capped at ~render cost (~60/s on game_of_life in CI).

- **Eight-word tone canon** — run the standing grep (WORKING_AGREEMENT § Tone canon) before any close. Clean at baseline (review #39); a Studio UI with many authored labels is the most likely place anthropomorphism/marketing-reframe drift would creep in.
- **`substrate.api`-only boundary** — the backend must never import substrate kernel internals. If a Studio feature tempts a kernel import, that is a finding (build the projection on the public surface instead, the established pattern).

- **Live run: top verdict "● LIVE" vs bottom health "INCOMPLETE (no terminal)" coexist (wider pass, LOW).** Two truthful framings of the same state (being-followed vs no-terminal-yet); mild surface inconsistency. Re-visit if it reads as contradictory; could align the wording.
- **~~Completed `ticker` bar reads amber (wider pass)~~ — RESOLVED, not a bug.** Confirmed in code: `.bar.completed` is green (`border:1px solid var(--green)`, green-tinted fill, index.html:76); `.spawn` is an 8px amber dot at the start mark (index.html:81). The ticker emits ONE LiveTick → bar width `Math.max(1.2,…)` ≈ 1.2% → the amber spawn dot sits over the tiny green bar. Correct rendering; the amber was the anchor, not the bar. No fix needed.
- **[FOLDED 2026-06-22 — sprint 012] `runs/` has no lifecycle management (perceptual pass, LOW).** DONE: **(C)** the rail groups "your runs" (newest-first) vs "demos" via a server `source` tag; **(C2)** a confirm-gated `clear` affordance + `POST /api/runs/clear` prunes the hash-suffixed session runs (KEEPING bundled demos + the `demo_*` fixtures), and the source tag was corrected to the `launch_/build_/resume_` prefix so the fixtures group as demos. Cleaned the 231-run detritus (`{removed:231, kept:8}`). ORIGINAL: every launch/build/resume wrote a durable record to `runs/`, enumerated flat with no cap/separation.

- **`unfired_triggers` false-positive on matured-but-input-build-failed Triggers (review #41, LATENT).** `/api/build` derives "fired" from the trigger_ids present on `run_graph` instances. But a Trigger whose Predicate matured yet whose `input_builder` raised/returned non-canonical output emits `substrate.InputBuildFailed` with NO `TriggerFired` and NO instance (`sequencer.py`: input_builder runs before TriggerFired is emitted) — so `unfired_triggers` would list it as "never fired" when it actually fired-but-failed-to-build-input, conflating two states the Studio will want to show differently. **NOT reachable today:** the stub's `input_builder` is `lambda ctx: ctx.event.payload` (already-canonical, can't raise). **Re-visit condition:** BEFORE real-model Producers / custom input_builders land — then derive "fired" from the `TriggerFired`/`InputBuildFailed` events keyed by trigger_id (not from run_graph instances), or segment the signal into "unreachable predicate" vs "fired-but-failed-input".

---

## Sprint tail

*Agent maintains. Last 10 increment closes; older roll into ## Built as compressed paragraphs.*

### Sprint 012 (2026-06-22, in progress) — drift-watchlist polish (B, C done; C2, A next)
- **Scope:** clear the Drift watchlist. B (rAF replay), C (rail grouping) done; C2 (prune + clean), A (glider fixture) next.
- **Honesty note (Architect-flagged):** B + C were built CODE-FIRST before the sprint card existed — a discipline miss. Corrected: `sprint-012-drift-polish.md` now declares the contract, and C2 + A are contract-before-code.
- **Rubber Duck Pass (B + C):**
  - *Narration:* B — the play loop is a rAF that accumulates (elapsed × speed) seqs and renders once/frame; at 480/s it advances ~8 seqs/frame, so a 200-seq tail clears in <0.5s (e2e proves it, the old ~60/s loop couldn't). C — the server tags `source=demo|run`; the rail renders a "your runs" group (newest-first by ULID) above "demos".
  - *Observations (six):* missing pair — none; order — C's newest-first uses the ULID `run_id` (time-sortable); B reaches every seq + stops at max; vocabulary gap — none ("your runs"/"demos"/play/speed are reader/control terms, not the eight primitives; grep clean); payload anomaly — none (`source` additive; 24 server tests unaffected); timing surprise — B's whole point is decoupling the replay rate from the render rate (no longer render-bound); tone trace — plain, on-vocabulary.
  - *Dispositions:* clean. The 239 accumulated runs were real detritus — addressed by C2 (prune + clean, `{removed:231,kept:8}`). Zero `halted`.
- **Rubber Duck Pass (C2 — prune):** *Observations:* the item-C `source` tag (`name in bundled.names()`) was WRONG — it grouped the 8 `demo_*` fixtures under "your runs"; caught here and corrected to the `launch_/build_/resume_` prefix (a real fix, not cosmetic). The prune deletes ONLY session runs, KEEPS demos+fixtures, SKIPS live records, and is an explicit confirm-gated action (review #35's no-SILENT-deletion ruling holds). Verified by a server test + the live `{removed:231,kept:8}`. Zero `halted`.
- **Files:** web/app.js, web/index.html, server.py, harness/e2e_console.js, tests/test_server.py, sprints/sprint-012-drift-polish.md
- **Rubber Duck Pass (A — glider):** *Observations:* the glider decodes clean across all 5 generations AND is mirror-sensitive on every frame (decoded != its L-R mirror), so the decoder's mirror-blind spot is genuinely closed — not just claimed. A real harness RACE surfaced (`openScene` read `STATE.scene` before the record loaded → previous record's seqs) and was fixed at root (API ground-truth seqs + a `STATE.events`-matches-max-seq wait), not papered over with a sleep. Zero `halted`.
- **Sprint 012 CLOSED:** all four drift items folded (B rAF, C grouping, C2 prune, A glider). Remaining `noted`: D (fold `capture_scene` into the canonical `e2e_console.js`/`capture_console.js`).

### Sprint 010 (2026-06-22, closed) — the integrated terminal (read interface) + seq/time pairing
- **Scope:** a read-interface terminal dock (tail/cat/ls/input/narrate/inspect) + seq+timestamp everywhere events are listed. Two files: web/index.html, web/app.js.
- **Dual contract:** artifact pass (node --check clean; eight-word grep clean; 24 server tests unaffected — no backend change); observation BOTH tracks — structural (e2e: cat prints the CodeChunk full payload; narrate prints beats + t+; stream shows t+) + perceptual (terminal.png captured AND VIEWED: color-coded narrate, seq + t+Δs, clean).
- **Rubber Duck Pass:**
  - *Narration:* opening the dock greets; `cat 3` on pair_coding prints the CodeChunk payload (`text: "def solve(x):\n"`) — the code, visible; `narrate` walks the beats (CodeChunk/ChunkBoundary/Suggestion + the substrate.* causal beats) each with seq + relative t; tail/ls/input/inspect read STATE.events / /io / /explain.
  - *Observations (six):* missing pair — none; order — none (reads the same seq-ordered record; seq+t together is the point); vocabulary gap — none (terminal/tail/cat/narrate/inspect are command verbs; the eight words intact; grep clean); payload anomaly — none (cat prints the real payload faithfully); timing surprise — t+ is ~0 on the deterministic CI demos (truthful — they ARE instant), real gaps on real-model runs; tone trace — plain, on-vocabulary.
  - *Dispositions:* clean. Terminal is READ-only this sprint (control/model deferred to the interactive-agent sprint) — `noted`. The GUI inspector still gists application content (terminal `cat` shows the full body) — `surfaced` to BACKLOG. Zero `halted`.
- **Files:** web/index.html, web/app.js, harness/e2e_console.js, process/BACKLOG.md, sprints/sprint-010-terminal.md
- **Next:** the interactive open-source-model agent in the terminal (run a model-backed topology, type to it, watch it grow live).

### Sprint 009 (2026-06-22, closed) — the replay Transport (play/pause/speed/rewind)
- **Scope:** play/pause/speed/rewind on the existing seq-cursor — the designed Transport the shipped console had dropped. Two files: web/index.html, web/app.js.
- **Dual contract:** artifact pass (node --check clean; eight-word grep clean; 24 server tests unaffected — no backend change); observation BOTH tracks — structural (e2e: play advances #seqnow 0->44, pause holds, stops at end 263==263, button ->▶, replay rewinds to ~0 seq 28) + perceptual (replay_controls.png captured AND VIEWED: green ⏸ playing, 80/s selector, seq 45/263 mid-replay, clean).
- **Rubber Duck Pass:**
  - *Narration:* the play-loop steps the one cursor (each tick = seq+1 via the existing `$("seq").oninput` path -> render); the graph/stream/scene update in lock-step; at max it stops; at-end play rewinds to 0; pointerdown on the slider + record-switch stop it.
  - *Observations (six):* missing pair — none; order — none (the cursor advances monotonically; per-seq rendering unchanged); vocabulary gap — none (play/pause/speed/replay are control verbs, not primitive renames; the eight words intact; grep clean); payload anomaly — n/a (reads, emits nothing); timing surprise — the nominal 240/s is render-bound to ~render cost (the per-tick render() caps it) — `noted`, not a defect; tone trace — controls on-vocabulary, no marketing.
  - *Dispositions:* clean. Render-bound top speed `noted` (Drift watchlist: rAF / skip-render-on-fast). Original-timing-via-`t` `deferred` (meaningful for real-model runs). Zero `halted`.
- **Files:** web/index.html, web/app.js, harness/e2e_console.js, sprints/sprint-009-replay-transport.md
- **Next:** the terminal (cat/narrate/tail read commands) -> the interactive open-source-model agent.

### Sprint 008 (2026-06-22, closed) — the scene panel (custom visual output)
- **Scope:** a third graph view "scene" — a cursor-driven, shape-detected domain visual (a 2-D numeric payload field -> a cell grid), opt-in by shape not app code. Two files: web/app.js, web/index.html.
- **Dual contract:** artifact pass (node --check clean; eight-word grep clean; 24 server tests unaffected — no backend change); observation BOTH tracks — structural (capture_scene.js: scene tab visible game_of_life / hidden code_review; 25 cells, 3 live; scrub changes positions) + perceptual (frames scene_gol_gen0/1/2 captured AND VIEWED: blinker oscillates vertical->horizontal->vertical, clean, on-vocabulary).
- **Rubber Duck Pass:**
  - *Narration:* selecting game_of_life -> updateScene detects Generation.grid -> the scene tab appears; clicking renders the latest Generation at/before the cursor; at seq 3 the vertical blinker (frame 1/3, tick=0), seq 132 horizontal (2/3, tick=1), seq 261 vertical again (3/3, tick=2) — the period-2 oscillation, animated by the one cursor.
  - *Observations (six):* missing pair — none; order — none (the scene reads the same seq-ordered events; latest-frame-at-cursor is monotonic); vocabulary gap — none (scene/cell/grid are not primitive names; the eight words intact; grep clean); payload anomaly — none (renders Generation.grid faithfully; 25 cells = 5×5); timing surprise — none; tone trace — caption + hint plain, on-vocabulary, no marketing.
  - *Dispositions:* clean. The structural + perceptual harness landed as one new capture_scene.js rather than split across e2e_console.js / capture_console.js — `noted` (Drift watchlist), an honest scoping choice, not a defect. Binary on/off cell rendering (0 vs non-0) covers the blinker; value->color mapping is a future extension — `noted`. Zero `halted`.
- **Files:** web/app.js, web/index.html, capture_scene.js, sprints/sprint-008-scene-panel.md
- **Next:** the duplex-pipe review of the scene panel; consider folding the scene assertions into the canonical e2e_console.js / capture_console.js.

### Sprint 007 (2026-06-17, closed) — Studio model-Producer authoring
- **Scope:** a model checkbox + prompt per Producer row + a responder selector. Two files: web/studio.html, web/studio.js.
- **Dual contract:** artifact pass (node --check clean; server tests 22 unaffected); observation BOTH tracks — structural (e2e_studio.js: model checkbox on each row, responder selector present, model judge build → Verdict carries stub[0]:… not "judge") + perceptual (frames 35/35b captured AND viewed: model controls + responder selector legible, on-vocabulary).
- **Rubber Duck Pass:**
  - *Narration:* buildSpec now emits producer.model/prompt + spec.responder; marking judge model + prompt "rate the critiques" + deterministic responder → build → the judge (started by adjudicate) calls DeterministicResponder(0).respond("rate the critiques") → Verdict{note=stub[0]:5bc9ea9bf74b} → finalised.
  - *Observations (six):* missing pair — none; order — none; vocabulary gap — none (a model-backed Producer is still a Producer — the eight words; grep clean); payload anomaly — none (Verdict carries the asserted responder output); timing surprise — none; tone trace — labels/hints on-vocabulary ("real LLM", "pure + seeded" — plain, not marketing).
  - *Dispositions:* clean. ollama selectable but unverified here (no Ollama in this env) — the deterministic path is fully tested; ollama is the documented real-LLM path with loud failure. `noted`. Zero `halted`.
- **Files:** web/studio.html, web/studio.js, e2e_studio.js, capture_studio.js, sprints/sprint-007-studio-model-authoring.md
- **Next:** the duplex-pipe review of sprints 005–007 (canvas + model Producers), per the Architect.

### Sprint 006 (2026-06-17, closed) — model-backed Producer seam
- **Scope:** a model:true Producer calls the runtime's real Responder; responder selectable (deterministic|ollama). Two files: builder.py, server.py.
- **Dual contract:** artifact pass (ruff clean); observation (test_build_model_producer_runs_the_responder — the DeterministicResponder output lands in the emitted payload, proving the real responder ran). 22 server tests pass.
- **Rubber Duck Pass:**
  - *Narration:* /api/build of a model topology (rater model:true prompt="rate this" → Verdict) with the default DeterministicResponder → RunStarted → ProducerStarted(rater) → Verdict{note=DeterministicResponder(0).respond("rate this")=stub[0]:…} → ProducerCompleted → TerminationMatched → RunFinalised.
  - *Observations (six):* missing pair — none; order — none; vocabulary gap — none (a model-backed Producer is still a Producer — the eight words; grep clean); payload anomaly — none (note carries the real responder output, asserted equal); timing surprise — none (DeterministicResponder pure/fast); tone trace — on-vocabulary.
  - *Dispositions:* the stub emit-once ceiling still applies to model Producers with DeterministicResponder (each emits once; the payload is real, the count is one) — `noted`, honest. responder:"ollama" with no Ollama → ProducerFailed (loud) — `noted`, by design. Zero `halted`.
- **Files:** builder.py, server.py, test_server.py, sprints/sprint-006-model-producer-seam.md
- **Next:** sprint 007 — the Studio authoring UI for model Producers (model checkbox + prompt + responder selector), with the two-track front-end observation contract. Then the review.

### Sprint 005 (2026-06-17, closed) — Studio drag-canvas
- **Scope:** a form↔canvas toggle + a draggable node-graph view of the authored spec. Two files: web/studio.html, web/studio.js.
- **Dual contract:** artifact pass (node --check clean; server tests 21 unaffected); observation BOTH tracks — structural (e2e_studio.js: 3 cards / 2 initial / 2 adjudicate edges / drag moved a card / form round-trips + validates) + perceptual (frame 34 element-captured AND viewed: clean node-graph, curved labelled edges, no overlap).
- **Rubber Duck Pass:**
  - *Narration:* the canvas reads buildSpec() → reviewer-a/reviewer-b (initial, green) + judge cards, two curved `adjudicate` Trigger arrows reviewers→judge; dragging a card updates CANVAS_POS and re-renders edges; toggling back to form preserves the authored rows.
  - *Observations (six):* missing pair — none; order — none; vocabulary gap — none (Producer cards / Trigger + Route edges — the eight words; grep clean; no "node/agent" in labels); payload anomaly — none (pure view of buildSpec); timing surprise — none; tone trace — labels on-vocabulary.
  - *Dispositions:* the canvas is a VIEW, not an editor this sprint (canvas-creation deferred) — `noted`, by design. Route edges approximate (of-emitter → triggered producers) since the stub declares no per-producer slot consumption — `noted`, labelled honestly. Zero `halted`.
- **Files:** web/studio.html, web/studio.js, e2e_studio.js, capture_studio.js, sprints/sprint-005-studio-drag-canvas.md
- **Next:** real-model Producers (sprint 006) — lift the deterministic-stub emit-once ceiling so authored Producers actually compute.

### Sprint 004 (2026-06-17, closed) — Studio Routes + termination composition
- **Scope:** add Routes + any_of/all_of composition authoring. Two files: web/studio.html, web/studio.js.
- **Dual contract:** artifact pass (node --check clean; server tests 21 unaffected; route+all_of probed valid→finalised through the real seam); observation BOTH tracks — structural (e2e_studio.js: 2 default members; route+all_of → valid → built·finalised) + perceptual (frame 33 captured AND viewed: ROUTES + member rows render cleanly, on-vocabulary).
- **Rubber Duck Pass:**
  - *Narration:* the Studio now emits `routes:[{id,of,slot}]` + `termination:{kind:any_of|all_of, members:[…]}`; a route stage-crit (of Critique → slot crit_ctx) + an all_of(all_completed, quiescence_with_watchdog(1)) validate through the real TopologyBuilder.build() and build a finalised run.
  - *Observations (six):* missing pair — none; order — none; vocabulary gap — none (Route/TerminationPolicy/member policies — the eight words; grep clean); payload anomaly — none (spec mirrors builder.py; probe confirmed); timing surprise — none; tone trace — on-vocabulary.
  - *Dispositions:* an unconsumed Route slot validates + builds (the Route stages, nothing reads it) — `noted`, honest by design, not an error. Zero `halted`.
- **Files:** web/studio.html, web/studio.js, e2e_studio.js, capture_studio.js, sprints/sprint-004-studio-routes-composition.md
- **Next:** the drag-canvas presentation (over the same spec) + real-model Producers — the last "full parity" pieces.

### Sprint 003 (2026-06-17, closed) — Studio authoring surface (form-first)
- **Scope:** the author→validate→build→view loop. 4 files: web/studio.html, web/studio.js (new), web/app.js (?record= deep-link), + e2e_studio.js/capture_studio.js harness.
- **Dual contract:** artifact pass (node --check clean; server tests 21 unaffected; studio.html served 200); observation BOTH tracks — structural (e2e_studio.js 8/8, incl. a real authored run that EXECUTED) + perceptual (frames 30/32 captured AND viewed: clean form, clear build result, on-vocabulary).
- **Rubber Duck Pass:**
  - *Narration:* /studio.html loads pre-filled (reviewer-a/reviewer-b initial → Critique; judge → Verdict; crits=KindCount(Critique); adjudicate on Critique when crits≥2 starts judge Once; any_of termination) → validate → `● valid` (real TopologyBuilder.build) → build → `● built · finalised  build_authored_demo_…` → deep-link → the console shows the built run with Critique×2 → Verdict (the authored Trigger fired). Negative: starts="ghost" → `● invalid  starts="ghost" — unknown Producer kind`.
  - *Observations (six categories):* missing pair — none; order violation — none; vocabulary gap — none (Producers/Views/Triggers/Predicates/TerminationPolicy — the eight words; grep clean); payload anomaly — none (the spec mirrors builder.py; the run executed correctly); timing surprise — none; tone trace — labels + hints on-vocabulary, no marketing.
  - *Dispositions:* clean. The deterministic stub's emit-once ceiling still applies (a count predicate above the producer count → unfired_triggers warning, shown honestly) — `noted`, by-design until real-model Producers. Zero `halted`.
- **Files:** web/studio.html, web/studio.js, web/app.js, e2e_studio.js, capture_studio.js, package.json, sprints/sprint-003-studio-authoring.md
- **Next:** Studio depth — Routes + any_of/all_of nested composition authoring; then a drag-canvas; then real-model Producers (lift the stub ceiling).

### Sprint 002 (2026-06-17, closed) — static topology-structure view
- **Scope:** wire `/topology_graph` into the console as a toggle beside the run-as-graph. Two files: `web/app.js` (renderTopology + toggle), `web/index.html` (toggle + CSS).
- **Dual contract:** artifact pass (node --check clean; server tests unaffected at 21); observation BOTH tracks pass — structural (E2E incl. topology-toggle assertions) + perceptual (frame 02b captured AND viewed: legible static structure, distinct from the run timeline, on the eight words).
- **Rubber Duck Pass:**
  - *Narration (the topology_graph projection as the "trace"):* the STRUCTURE view renders 6 Producers (5 `reviewer-*` ▸ initial emitting CritiquePosted, `judge` non-initial emitting VerdictRendered) → the `adjudicate` Trigger (on CritiquePosted → starts judge, Once) → the `critiques` View → no Routes → TerminationPolicy `any_of(cancel_all_others, all_completed, quiescence_with_watchdog(60.0))`. Seq-independent by design.
  - *Observations (six categories):* missing pair — none; order violation — none; vocabulary gap — none (Producers/Triggers/Views/Routes/TerminationPolicy — the eight words; grep clean); payload anomaly — none (renders the projection faithfully); timing surprise — none (static structure, correctly seq-independent); tone trace — labels on-vocabulary.
  - *Dispositions:* the event stream still renders BELOW the static structure (mixes static + dynamic in one pane) — `noted`, acceptable (the stream is record context, always useful); not a finding. All else clean. Zero `halted`.
- **Files:** web/app.js, web/index.html, e2e_console.js, capture_console.js, sprints/sprint-002-static-topology-view.md
- **Next:** the Studio authoring canvas (the bigger E2-ruling piece) remains the major open build — now on a console that ships BOTH §6 read surfaces.

### Sprint 001 (2026-06-17, closed) — Studio build seam under test + honest
- **Scope:** the two translator tests + the stub honesty fix. Two concept-files: `test_server.py` (coverage), `builder.py`/`server.py` (the unfired-Trigger surface).
- **Dual contract:** artifact pass (`test_server.py` 21 passed; ruff clean on builder/server/tests); observation pass (the authored build records prove the Trigger fired). Live E2E not run this env (Playwright absent) — backend-only, flagged honest.
- **Rubber Duck Pass:**
  - *Sequence narration (the `/api/build` of `_AUTHORED` as the signal trace):* `substrate.RunStarted` → `TriggerFired{__initial__}` ×2 (reviewer-a, reviewer-b) → `ProducerStarted` ×2 → `Critique`(note=reviewer-a) → `Critique`(note=reviewer-b) → at quorum the `crits` View `KindCount` reaches 2 → `TriggerFired{trigger_id=adjudicate}` (Once) → judge `ProducerStarted` → `Verdict` → `TerminationMatched{any_of(all_completed, quiescence_with_watchdog)}` → `RunFinalised`. The unreachable-spec trace is identical up to the initials, then quiesces with `needs_three` never firing (no judge/`Y`).
  - *Observations (six categories):* missing pair — none (every `ProducerStarted` has its `ProducerCompleted`). order violation — none (`RunStarted` first, `RunFinalised` terminal). vocabulary gap — none (the UI emits no signals; the tests ASSERT on substrate's v0.2 kinds). payload anomaly — none. timing surprise — the unreachable case finalising green is the designed quiescence, now SURFACED via `unfired_triggers` rather than silent (the honesty fix). tone trace — `unfired_triggers` is plain vocabulary (Trigger, not "rule"/"step"); grep clean.
  - *Dispositions:* the silent-green ceiling — `resolved-here` (surfaced in the build result + docstring + test). All else clean. Zero `halted`.
- **Files:** test_server.py, builder.py, server.py, sprints/sprint-001-studio-build-seam.md
- **Next:** sprint 002 — the visual canvas (the front-end authoring surface) against this now-tested seam. Send #39's fixes to the reviewer to CONFIRM (the runbook's find-AND-confirm discipline) before building the canvas.

---

*BLACKBOARD.md for substrate-ui. Instantiated as the review-#39 retrofit. Rounds #30–#38 pointed-to, not re-derived. From the Studio increment forward: a sprint card with a declared dual+observation contract before code.*
