# Rationale — signal vocabulary v0.5

*New file per no-in-place-edits. `0.4-rationale.md` stays on disk. v0.5 adds one tag — `PRODUCER_INSPECTION_RACED` — to close the M4 finding from `REVIEW-2026-08-16-sdd-instrumentation-comprehensive.md`.*

---

## Per-change ledger (v0.5 vs v0.4)

### 1. New tag — `PRODUCER_INSPECTION_RACED`

- **Category:** stream (same as PRODUCER_INSPECTED).
- **Stratum:** incident.
- **Payload:** `instance`, `subject_record`.
- **Note:** User clicked a producer lane whose kind wasn't yet in `STATE.graph.instances` — the click landed before the graph fetch settled. The inspector still opens the `/explain/<instance>` chain; the emit records the race so it stays visible on the record instead of dropping into silence.

Emit site: `web/app.ts § inspectProducer`, the else branch of the `if (laneInst && laneInst.kind)` guard.

## Why v0.5 was direct rather than halted

The v0.4 rationale deferred this tag on halt-and-articulate grounds. The Architect authorized the direct add 2026-08-17 with the directive "fix M4 if it makes sense. Don't do halt-and-articulate, just do the new tag." One tag, one emit site, no other changes.

## What did NOT change

- Every prior tag from v0.4 (53 of them).
- Every invariant, entity_merge, entity_merges_ratification record, runtime_operator, external_vocabulary_ref, payload_types entry.
- Total tag count: 54 (was 53).

## Signatures

- Agent (drafter): Claude Opus 4.7, 2026-08-17.
- Architect (ratifier): Peter, via directive 2026-08-17.
